# Quality Attribute Scenarios

Last updated: 2026-05-25

| ID | Attribute | Scenario | Measure | Verification | Linked Requirements | Status | Last Updated |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QAS-DOC-005 | maintainability/traceability | Agents should be able to inspect a feature as a full graph-backed chain rather than a local file, including UI, API, service, data, tests, docs, and missing evidence. | CSV graph generation succeeds; generated Obsidian nodes and JSON export exist; seed chains expose missing proof instead of hiding it. | `pnpm run architecture:graph:generate` passed with `45` nodes, `24` relations, and `4` chains. Current coverage is a seed and not full repository backfill. | REQ-DOC-005 | implemented_not_verified | 2026-05-24 |
| QAS-DOC-030 | maintainability/traceability/QA | A UI route or control change should be traceable from user action to API routes, function chains, backend functions, data models, tests, docs, evidence, safety boundary, and next proof before closure. | Generated action index has zero critical action gaps; triage command returns linked API/backend/data/test/proof context for a representative action. | `pnpm run architecture:journey:index` generated `39` user actions, `0` critical action gaps, and `37` high proof gaps; `pnpm run architecture:journey:triage -- --query SOAR-UI-MANUAL-ORDER-SUBMIT` returned `/dashboard`, `SOAR-API-ORDER-OPEN`, manual-order chains, backend services, DB models, tests, docs, and proof gaps. | REQ-DOC-030 | verified_local | 2026-05-25 |
| QAS-DOC-006 | maintainability/impact-analysis | A high-risk manual-order change should be traceable across UI, client service, API route, controller, validation schema, backend service, pre-trade, exchange boundary, lifecycle, DB models, tests, and docs. | The detailed manual-order chain exists and graph validation has zero missing node or file references. | `CHAIN-MANUAL-ORDER-DEEP` was added; `pnpm run architecture:graph:generate` passed with `67` nodes, `51` relations, and `5` chains; guardrails and docs parity passed. | REQ-DOC-006 | partially_verified | 2026-05-24 |
| QAS-DOC-007 | maintainability/impact-analysis | A high-risk position change should be traceable across legacy route behavior, runtime UI, Web service, API route, controller, validation schema, backend service, exchange snapshot, reconciliation, DB models, tests, and docs. | The detailed Positions core chain exists and graph validation has zero missing node or file references. | `CHAIN-POSITIONS-CORE` was added; `pnpm run architecture:graph:generate` passed with `93` nodes, `80` relations, and `6` chains. | REQ-DOC-007 | verified | 2026-05-24 |
| QAS-DOC-008 | maintainability/impact-analysis | A high-risk Bot Runtime change should be traceable across route, runtime UI, Web service, API route, controller, validation schema, aggregate/read/command services, DB models, tests, and docs. | The detailed Bot Runtime core chain exists and graph validation has zero missing node or file references. | `CHAIN-BOT-RUNTIME-CORE` was added; `pnpm run architecture:graph:generate` passed with `115` nodes, `103` relations, and `7` chains. | REQ-DOC-008 | verified | 2026-05-24 |
| QAS-DOC-009 | maintainability/impact-analysis | A high-risk exchange integration change should be traceable across capability contracts, authenticated/public read boundaries, connector factory, live order adapter, symbol rules, fee reconciliation, consumers, tests, and docs. | The detailed Exchange Adapter chain exists and graph validation has zero missing node or file references. | `CHAIN-EXCHANGE-ADAPTER-DEEP` was added; `pnpm run architecture:graph:generate` passed with `142` nodes, `129` relations, and `8` chains. | REQ-DOC-009 | verified | 2026-05-24 |
| QAS-DOC-010 | maintainability/impact-analysis | A high-risk wallet/capital-context change should be traceable across wallet routes, components, Web service, API routes, controller, DTOs, wallet service, exchange read/boundary dependencies, ledger/cashflow services, DB models, tests, and docs. | The detailed Wallets core chain exists and graph validation has zero missing node or file references. | `CHAIN-WALLETS-CORE` was added; `pnpm run architecture:graph:generate` passed with `176` nodes, `177` relations, and `9` chains. | REQ-DOC-010 | verified | 2026-05-24 |
| QAS-DOC-011 | maintainability/impact-analysis/security-traceability | A high-risk credential handling change should be traceable across profile API-key UI, Web service, API routes, controller, DTOs, encrypted storage, probe service, exchange boundary, audit log, Wallets/Bot Runtime consumers, tests, and docs. | The detailed Profile API Keys chain exists and graph validation has zero missing node or file references. | `CHAIN-PROFILE-API-KEYS` was added; `pnpm run architecture:graph:generate` passed with `202` nodes, `212` relations, and `10` chains. | REQ-DOC-011 | verified | 2026-05-24 |
| QAS-DOC-012 | maintainability/impact-analysis | A high-risk bot setup or topology change should be traceable across bot list/create/edit UI, Web service, lifecycle API routes, controller, DTOs, validation services, activation policy, canonical topology DB models, Wallets/Profile API Keys dependencies, tests, and docs. | The detailed Bot Setup chain exists and graph validation has zero missing node or file references. | `CHAIN-BOT-SETUP` was added; `pnpm run architecture:graph:generate` passed with `229` nodes, `251` relations, and `11` chains. | REQ-DOC-012 | verified | 2026-05-24 |
| QAS-DOC-013 | maintainability/impact-analysis | A high-risk strategy authoring or runtime-selection change should be traceable across strategy list/create/edit UI, Web service, form mapping, presets, indicator catalog, API routes, controller, DTO/validation, service, DB guards, Bot Setup and Bot Runtime consumers, tests, and docs. | The detailed Strategies chain exists and graph validation has zero missing node or file references. | `CHAIN-STRATEGIES` was added; `pnpm run architecture:graph:generate` passed with `261` nodes, `293` relations, and `12` chains. | REQ-DOC-013 | verified | 2026-05-24 |
| QAS-DOC-014 | maintainability/impact-analysis | A high-risk market universe or symbol-scope change should be traceable across market list/create/edit UI, Web service, helper utilities, catalog endpoint, API routes, controller, DTOs, service, exchange-catalog/symbol resolver, DB guards, Bot Setup and Bot Runtime consumers, tests, and docs. | The detailed Markets chain exists and graph validation has zero missing node or file references. | `CHAIN-MARKETS` was added; `pnpm run architecture:graph:generate` passed with `286` nodes, `329` relations, and `13` chains. | REQ-DOC-014 | verified | 2026-05-24 |
| QAS-DOC-015 | maintainability/impact-analysis/parity | A high-risk backtest replay or report change should be traceable across list/create/detail UI, Web service, API routes, controller, DTOs, service, queue/job, replay/data/fill/report services, DB models, Strategy/Market dependencies, Reports consumer, tests, and docs. | The detailed Backtests chain exists and graph validation has zero missing node or file references. | `CHAIN-BACKTESTS` was added; `pnpm run architecture:graph:generate` passed with `324` nodes, `371` relations, and `14` chains. | REQ-DOC-015 | verified | 2026-05-24 |
| QAS-DOC-016 | maintainability/impact-analysis/operator-truth | A high-risk Reports change should be traceable across reports route, PerformanceReportsView, Web reports/backtests services, cross-mode API route, controller, backend service, aggregation utility, backtest/trade/bot read models, tests, and docs. | The detailed Reports chain exists and graph validation has zero missing node or file references. | `CHAIN-REPORTS` was added; `pnpm run architecture:graph:generate` passed with `336` nodes, `396` relations, and `15` chains. | REQ-DOC-016 | verified | 2026-05-24 |
| QAS-DOC-017 | maintainability/impact-analysis/auditability | A Logs/Audit Trail change should be traceable across route, AuditTrailView, Web service, API route, controller, query schema, service, Log model, event producer links, tests, and docs. | The detailed Logs/Audit Trail chain exists and graph validation has zero missing node or file references. | `CHAIN-LOGS-AUDIT` was added; `pnpm run architecture:graph:generate` passed with `349` nodes, `413` relations, and `16` chains. | REQ-DOC-017 | verified | 2026-05-24 |
| QAS-DOC-018 | maintainability/impact-analysis/security-traceability | A Subscriptions/Admin entitlement or management change should be traceable across admin/profile UI, Web services, admin and profile subscription APIs, controllers, DTO schemas, subscription services, entitlement guard, checkout persistence, DB models, tests, and docs. | The detailed Subscriptions/Admin chain exists and graph validation has zero missing node or file references. | `CHAIN-SUBSCRIPTIONS-ADMIN` was added; `pnpm run architecture:graph:generate` passed with `387` nodes, `463` relations, and `17` chains. | REQ-DOC-018 | verified | 2026-05-24 |
| QAS-DOC-019 | maintainability/impact-analysis/AI-governance | An AI Assistant foundation change should be traceable across assistant UI, Web service, assistant APIs, controller schemas, BotAssistantService, AssistantOrchestrator, assistant config/subagent DB models, tests, docs, red-team agent, and prompt protocol. | The detailed AI Assistant foundation chain exists and graph validation has zero missing node or file references. | `CHAIN-AI-ASSISTANT-FOUNDATION` was added; `pnpm run architecture:graph:generate` passed with `411` nodes, `499` relations, and `18` chains. | REQ-DOC-019 | verified | 2026-05-24 |
| QAS-DOC-020 | maintainability/drift-detection | Agents should be able to see which representative source/test/doc/config/pipeline files are not yet referenced by architecture graph CSV records. | A deterministic drift audit produces Markdown and JSON outputs with covered/missing counts and samples by category. | `pnpm run architecture:graph:drift` passed with `796` inventoried files, `395` covered, and `401` missing graph path references. | REQ-DOC-020 | verified | 2026-05-24 |
| QAS-DOC-021 | maintainability/operations-traceability | Operations config and pipeline changes should be traceable across package manifests, workspace manifest, compose topology, CI workflow, guardrails, and local/testing/deployment docs. | The detailed Operations config/pipeline chain exists and drift audit reports `configAndPipelines` fully covered. | `CHAIN-OPS-CONFIG-PIPELINE` was added; `pnpm run architecture:graph:generate` passed with `426` nodes, `519` relations, and `19` chains; `pnpm run architecture:graph:drift` reports `configAndPipelines` as `9/9` covered. | REQ-DOC-021 | verified | 2026-05-24 |
| QAS-FULL-READINESS-2026-05-23 | reliability/release-truth | A coordinator responding to a "100%" readiness request must separate public reachability, deploy freshness, local source state, protected production evidence, deferred product scope, and approval-gated live-money proof before reporting completion. | The mission records current local/origin SHA alignment, public smoke result, deploy freshness, blocker categories, and explicit non-claims for protected auth, native mobile, AI hot-path trading, and LIVE mutation. | `PROD-FRESH-DEPLOY-380308D1-2026-05-24`: public reachability and deploy freshness are restored. Web/API/workers run `380308d10cf0fabb2ea629eb55e6f0ba7d980ed1`; public build-info returns that SHA with `metadataSource=github-branch`; public no-worker smoke passes API `/health`, API `/ready`, and Web `/`. No-secret V1 preflight rerun passes build-info and public smoke, then blocks only on protected auth/context and missing release evidence gates. | REQ-FUNC-021 | partially_verified | 2026-05-24 |
| QAS-LOCAL-DOCKER-COOLIFY-PARITY-2026-05-24 | reliability/operations | Local Docker startup should mirror the Coolify split-service shape before VPS topology, Dockerfile, worker ownership, or deploy-smoke changes are promoted. | Root commands render/build/start API, Web, and four split worker containers through the same compose topology documented for Coolify, with local-only env material and cleanup evidence. | `LOCAL-DOCKER-COOLIFY-PARITY-2026-05-24`: compose config PASS, Docker build PASS for API/Web/four worker images, short local container run PASS for API `/health` `200`, API `/ready` `200`, Web `/` `200`, guardrails PASS, strict graph drift PASS (`796/796`, `0` missing), app/worker cleanup PASS. | REQ-FUNC-021 | verified | 2026-05-24 |
| QAS-PROD-UI-LEGACY-REDIRECTS-2026-05-24 | usability/release-proof | Legacy dashboard links should reach canonical replacement surfaces and production UI audits should fail if they regress to 404. | Middleware tests cover authenticated legacy redirects and unauthenticated login redirect; production UI clickthrough includes those paths. | `PROD-UI-LEGACY-DASHBOARD-REDIRECTS-2026-05-24`: local middleware tests PASS (`4/4`), full Web tests PASS (`150` files / `535` tests), deploy `0b7eb4c6` PASS, public smoke PASS, production UI clickthrough PASS. | REQ-FUNC-019 | verified | 2026-05-24 |
| QAS-DOC-004 | maintainability/navigation | Each current docs file should have a nearest semantic area hub, so humans and agents can browse by domain while Obsidian shows coherent clusters with meaningful hub labels. | No current docs markdown file is fully isolated, no current docs markdown file lacks incoming links except root-level semantic hubs, and no `README.md` or `index.md` files remain under `docs/` or `history/`. | Orphan-link scan after semantic hub naming: `260` docs markdown files, `0` no-incoming files excluding `docs/soar-documentation-map.md` and `docs/documentation-overview.md`, and `0` fully isolated docs files. Markdown link check `1812` files / `0` missing targets; no `README.md` or `index.md` files under `docs/` or `history/`; `pnpm run quality:guardrails` PASS; `pnpm run docs:parity:check` PASS; `git diff --check` found no whitespace errors, only Windows CRLF warnings. | REQ-DOC-004 | verified | 2026-05-23 |
| QAS-DOC-003 | maintainability/navigation | Docs entrypoints should act as curated routers rather than dense graph hubs, so Obsidian visual structure stays readable and agents can distinguish primary routes from secondary references. | Broad docs hubs stay intentionally sparse: `docs/soar-documentation-map.md` and `docs/maps/*` use markdown links only for high-signal navigation and plain code paths for secondary references. | Link-density scan after content tuning: top outgoing docs hub is `10` links; `docs/soar-documentation-map.md` is `6`; `docs/maps/*` pages are `4-6`. Markdown link check `1805` files / `0` missing targets, `pnpm run quality:guardrails` PASS, and `pnpm run docs:parity:check` PASS. | REQ-DOC-003 | verified | 2026-05-23 |
| QAS-DOC-002 | maintainability/navigation | Historical records should be retrievable by semantic intent, not by a broad planning/operations bucket. | `history/tasks`, `history/plans`, `history/audits`, `history/evidence`, `history/releases`, and `history/artifacts` have semantic history hubs and no stale references to the old buckets remain outside move manifests. | Exact taxonomy stale-path scan `0`, markdown link check `1804` files / `0` missing targets, no dated markdown leftovers in `docs/`, `pnpm run quality:guardrails` PASS, and `pnpm run docs:parity:check` PASS. | REQ-DOC-002 | verified | 2026-05-23 |
| QAS-DOC-001 | maintainability/navigation | Agents and humans opening the repository in Obsidian should see durable docs maps and current source-of-truth docs without dated work logs dominating the graph. | Dated planning/operations work records are under `history/`; `docs/` contains current docs, maps, runbooks, and active planning; moved-path references resolve to existing history paths; generated evidence defaults to history. | `1788` historical files moved to `history/`; exact moved-path scan found `0` stale old paths; markdown link check covered `1732` docs/history files with `0` missing relative targets; `pnpm run quality:guardrails` PASS; `pnpm run docs:parity:check` PASS. | REQ-DOC-001 | verified | 2026-05-23 |
| QAS-LIVE-TRADING-SAFETY-2026-05-23 | money-safety/reliability | LIVE manual and automated bot order paths must calculate exchange-specific minimums, notional, runtime sizing, funds checks, PnL thresholds, and close lifecycle responses with the same venue and contract semantics before exchange placement or completion claims. | For Binance futures `contractSize=1` behavior remains unchanged; for Gate.io swap symbols with `contractSize>1`, displayed/checked notional and runtime sizing use `quantity * markPrice * contractSize`, symbol resolution chooses the swap market over same-id spot markets, reused submitted close dedupe responses remain `submitted` until fill completion, and `EXCHANGE_SYNC` imported LIVE DCA threshold evaluation uses exchange PnL truth when available. | Focused local tests passed: exchange/orders/runtime pack `9` files / `129` tests, API typecheck, guardrails, and diff check. Read-only Gate.io ADAUSDT swap probe showed `minAmount=1`, `contractSize=10`, one contract about `2.421 USDT`. Additional DB-backed manual-order service and route proof passed for Gate.io futures context: `quantity=4`, `contractSize=10`, `markPrice=0.25` => `10 USDT` notional and `2 USDT` margin at leverage `5`; combined route/service/orchestrator pack passed (`6` files / `98` tests). `RUNTIME-DCA-EXCHANGE-PNL-THRESHOLD-2026-05-23` adds focused exchange-PnL threshold proof: runtime automation tests `38/38`, DCA/position parity tests `27/27`, API typecheck, guardrails, docs parity, and diff check. Production public deploy proof for `9d1a8387`, docs/state HEAD `a0e4f117`, and Web follow-up `b703b67f` passed build-info freshness and no-worker smoke; protected manual/bot readbacks remain blocked without transient Soar app auth. No production LIVE mutation was attempted after this finding. | REQ-LIVE-EXCHANGE-PARITY-2026-05-23 | partially_verified_prod_public | 2026-05-23 |
| QA-039 | reliability/operations | Current protected V1 release evidence must use real open runtime payloads and fail closed when a requested symbol has no qualifying runtime readback. | Build-info, deploy smoke, split-worker readiness, restore, rollback, production UI, RC gates, and SLO health objectives are not enough by themselves; release readiness also requires `LIVEIMPORT-03` against qualifying open runtime position/order payloads, preferably through production auto-discovery to avoid stale symbol assumptions. | 2026-05-23 production proof: deployed SHA `b1ba69edccc639e97943f37fb2b1e6249a62e87c`; `LIVEIMPORT-03 --symbols auto` PASS for actual open runtime symbols `SOLUSDT` and `BNBUSDT`, both `EXCHANGE_SYNC`, `BOT_MANAGED`, `OWNED_AND_MANAGED`, and `IN_SYNC`; final preflight ready; full non-dry-run release gate ready. Follow-up `db07214667a700ba02ea8210629655cf2becd7d6` build-info converged after Coolify `soar-web` force start and authenticated deploy smoke passed. | REQ-FUNC-021 | verified | 2026-05-23 |
| QA-027 | security/money-flow fail-closed | When an exchange-backed LIVE order cancel is attempted after the user's active subscription no longer allows live trading, the API should reject before exchange adapter invocation and before local order mutation. | Downgraded FREE user receives `SubscriptionFeatureUnavailableError`, `cancelLiveOrder` is not called, and persisted order remains `OPEN`; allowed entitlement path still reaches the canonical exchange cancel boundary. | Parent verification reran `corepack pnpm --dir apps/api exec vitest run src/modules/orders/orders.liveCancelBoundary.service.test.ts src/modules/profile/apiKey/apiKey.e2e.test.ts --pool=forks --maxWorkers=1 --minWorkers=1 --test-timeout 30000` with local Postgres/Redis; `2` files / `20` tests passed. API/Web typecheck, full Web test pack, guardrails, build, audit, compose config, and diff check also passed. | REQ-SEC-040 | verified | 2026-05-21 |
| QA-000 | process | Agents verify significant claims before reporting completion. | Completed missions link evidence to requirement IDs. | inspection | REQ-FUNC-000 | verified | 2026-05-11 |
| QA-001 | usability/safety | When an operator deletes an active PAPER bot in the Bots UI, the app should not show a misleading LIVE-risk confirmation; when the bot is LIVE or live-opt-in, the LIVE confirmation must still appear. | Active PAPER delete calls `deleteBot`; LIVE delete cancellation does not call `deleteBot`. | component tests in `BotsManagement.test.tsx` | REQ-FUNC-001 | partially_verified | 2026-05-11 |
| QA-002 | usability/operator truth | Dashboard Home renders loading, retryable error, and empty/onboarding states; when an operator switches the selected bot, wallet KPIs and runtime table tabs update to the selected bot without stale rows. | Loading shell is aria-busy; error shell exposes retry; selected bot changes to the new bot; wallet KPI values update; Orders and History tabs show selected-bot symbol rows and suppress previous-bot rows; authenticated empty/onboarding state renders on desktop and mobile with keyboard focusable next action and no console errors; active PAPER snapshot state renders bot configuration, PAPER mode, wallet baseline, symbols, responsive desktop/tablet/mobile layouts, open position rows, safe tab interaction, backend-null trade relationship IDs as `-`, backend `origin=USER` as Manual in the edit-position context, aggregate `openPositionQty` in success/empty contracts, and runtime enum values that match backend fee/origin/management/capital domains. | rendered component test in `HomeLiveWidgets.runtime-table-audit.test.tsx`; local browser proof in `V1-DASHBOARD-HOME-BROWSER-PROOF-2026-05-11`; active snapshot browser proof in `V1-DASHBOARD-HOME-RUNTIME-SESSION-FIXTURE-2026-05-11`; `V1-WEB-BACKEND-PARITY-DASHBOARD-2026-05-13` focused Web tests (`17/17`), API aggregate e2e (`18/18`), and API/Web typechecks; `V1-WEB-BACKEND-PARITY-RUNTIME-ORIGIN-2026-05-13` focused Web test (`3/3`) and Web typecheck; `V1-WEB-BACKEND-PARITY-RUNTIME-ENUMS-2026-05-13` focused Web runtime tests (`47/47`), Web typecheck, stale-value scan, and guardrails | REQ-FUNC-002 | partially_verified | 2026-05-13 |
| QA-003 | usability/operator truth | Bot Runtime monitoring renders the selected bot's PAPER running and completed sessions with runtime status, wallet summary, symbols, positions, worker telemetry, and legacy route continuity through the same API contracts the UI consumes. | Canonical preview route shows selected bot `asd`, `RUNNING`, `PAPER`, the three imported symbols, wallet/portfolio KPI text, and no console issues for running proof; desktop/tablet/mobile render runtime symbols; safe monitoring view switch works; both legacy runtime URLs redirect to preview; completed filter renders `COMPLETED`, `0 open`, symbols, and wallet totals; worker live-loop e2e proves runtime-produced sessions/events/symbol stats/aggregate readbacks for `BTCUSDT`; monitoring trade table does not crash when backend returns nullable trade relationship IDs; runtime position types include backend `USER` origin; runtime trade fixtures and types reject impossible backend enum values; monitoring props reuse shared runtime enum aliases; production inventory truth is not overstated when requested LIVE/Gate.io resources are missing. | local API readbacks and authenticated browser proof in `V1-BOT-RUNTIME-PAPER-SESSION-BROWSER-PROOF-2026-05-11` and `V1-BOT-RUNTIME-COMPLETED-SESSION-FIXTURE-2026-05-11`; `runtime-flow.e2e.test.ts` in `V1-BOT-RUNTIME-WORKER-TELEMETRY-PROOF-2026-05-11`; `V1-WEB-BACKEND-PARITY-DASHBOARD-2026-05-13` focused Web tests (`17/17`) and API aggregate e2e (`18/18`); `V1-WEB-BACKEND-PARITY-RUNTIME-ORIGIN-2026-05-13` focused Web test (`3/3`); `V1-WEB-BACKEND-PARITY-RUNTIME-ENUMS-2026-05-13` focused Web runtime tests (`47/47`), Web typecheck, stale-value scan, and guardrails; `V1-WEB-BACKEND-PARITY-MONITORING-PROPS-2026-05-13` focused `BotsManagement` test (`14/14`), Web typecheck, duplicate-union scan, and guardrails; `V1-PRODUCTION-RUNTIME-INVENTORY-00169D7F-2026-05-13` production inventory `PARTIAL` | REQ-FUNC-003 | partially_verified | 2026-05-13 |
| QA-004 | security/session safety | Auth fails closed when a session is logged out, expired, deleted, duplicated, or missing on a protected route. | Logout clears cookie and next `/auth/me` returns 401; expired JWT clears cookie and returns session-expired message; deleted user session returns session-expired; newest duplicate token wins; web interceptor redirects protected routes to `/auth/login?session=expired`; middleware redirects protected routes without token to login. | `auth.e2e.test.ts` (`11/11`); focused Web Auth tests (`5` files, `17` tests) in `V1-AUTH-SESSION-LIFECYCLE-PROOF-2026-05-11` | REQ-FUNC-004 | partially_verified | 2026-05-11 |
| QA-005 | security/secrets | Profile API Keys never expose raw API secrets in storage responses, audit metadata, or connection-test flows. | Stored key and secret are encrypted; create/list responses mask keys and omit secret; provided connection tests leave no API key row; audit metadata contains exchange/probe mode/status/latency but not raw key material; owner-only mutation/test actions hold; production disposable proof creates a masked key, runs a fail-closed stored probe, verifies the audit event, and cleans up without writing raw secrets. | `apiKey.e2e.test.ts` and `exchangeApiKeyProbe.service.test.ts` (`25/25`); `ApiKeyForm.test.tsx` and `ApiKeysList.test.tsx` (`13/13`) in `V1-PROFILE-API-KEYS-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS | REQ-FUNC-005 | verified | 2026-05-14 |
| QA-006 | security/usability | Profile basic and security forms give honest success/error feedback while API routes enforce authenticated self-service boundaries. | Web save failure leaves the form usable and shows an error; Web password mismatch does not call the API; valid password change sends only current/new password and clears fields; API rejects unauthenticated security access and invalid timezone/weak-password flows; production disposable proof reads, reversibly updates, and restores the profile. | `basic.e2e.test.ts` and `security.e2e.test.ts` (`7/7`); `BasicForm.test.tsx` and `Security.test.tsx` (`5/5`) in `V1-PROFILE-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS | REQ-FUNC-006 | verified | 2026-05-14 |
| QA-007 | capital safety/usability | Wallet actions expose honest capital-source state and fail closed for unsafe ownership, active-bot, preview, and reset scenarios. | API blocks cross-user wallets/API keys, active-bot edits/deletes, unsupported preview, non-PAPER reset, reset with active positions/orders; Web surfaces mode-specific fields, preview/ledger states, reset success/error, and unavailable ledger empty state; production fixture proof verifies disposable wallet create/update/readback/cleanup and post-V1 hardening verifies active-bot reset blocking. | API Wallets tests (`43/43`) and Web Wallets tests (`22/22`) in `V1-WALLETS-LOCAL-PROOF-2026-05-11`; `history/tasks/v1-post-v1-wallet-bot-cleanup-hardening-2026-05-14-task.md`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS | REQ-FUNC-007 | verified | 2026-05-14 |
| QA-008 | runtime scope safety/usability | Market universe actions preserve canonical symbol scope and fail closed when a universe is still tied to active execution. | API blocks update/delete for active bot scope, permits inactive bot edits, ignores stale legacy links, preserves ownership isolation, and keeps placeholder catalog unsupported paths explicit; Web preview matches canonical composition and surfaces validation; production fixture proof verifies disposable universe create/update/catalog read/cleanup. | API Markets e2e (`17/17`) and Web Markets tests (`12/12`) in `V1-MARKETS-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS | REQ-FUNC-008 | verified | 2026-05-14 |
| QA-009 | trading config safety/usability | Strategy authoring preserves validated rule configuration and fails closed when a strategy is still tied to active execution. | API blocks update/delete for active bot strategy links, permits inactive bot updates, preserves ownership isolation, rejects invalid imports/configs, and exposes indicators deterministically; Web clone/form/preset/indicator flows map validated payloads without losing operator intent; the edit page submits the loaded form when backend allows the inactive linked-bot path and renders active-bot lock recovery when blocked; production fixture proof verifies disposable strategy create/export/update, bot link/readback, backtest compatibility, and cleanup. | API Strategies tests (`17/17`) and Web Strategies tests (`46/46`) in `V1-STRATEGIES-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS; `history/evidence/post-v1-inactive-paper-strategy-edit-proof-2026-05-14-task.md` Web edit page PASS (`3/3`), Web strategies suite PASS (`14` files, `48` tests), API strategies e2e PASS (`11/11`) | REQ-FUNC-009 | verified | 2026-05-14 |
| QA-010 | order safety/usability | Manual order actions expose honest lifecycle state and fail closed when price truth, scope, ownership, or live-risk context is unsafe. | API keeps manual context scoped to the selected bot, fills PAPER market orders only from canonical price truth, blocks unsafe reverse/live/exchange-backed paths, and preserves ownership; Web surfaces submitted, waiting, ready, imported, position-opened, blocked, and cancel states without hiding backend errors. | API Manual Orders tests (`75/75`) and Web Manual Orders tests (`20/20`) in `V1-MANUAL-ORDERS-LOCAL-PROOF-2026-05-11` | REQ-FUNC-010 | partially_verified | 2026-05-11 |
| QA-011 | position safety/operator truth | Position surfaces expose owned, current lifecycle truth and fail closed when stale, ambiguous, unsupported, or unsafe live contexts appear. | API scopes lists/status/snapshots/takeover/update/close to the owner, excludes stale local active rows, preserves ambiguous takeover diagnostics, and refuses unsafe stale or exchange-backed mutation; Web distinguishes ignored/closed/pending close states and derives runtime PnL truth safely; production proof validates PAPER-only open/read/update/close lifecycle, close-without-ack rejection, terminal closed readback, and OPEN-list cleanup. | API Positions tests (`90/90`) and Web Positions tests (`10/10`) in `V1-POSITIONS-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-positions-proof-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-011 | verified | 2026-05-14 |
| QA-012 | order lifecycle safety/operator truth | Order surfaces expose active lifecycle truth and fail closed when fills, fees, exchange-backed cancel, or live-risk context are unsafe. | API preserves ownership, active-only filtering, exchange event fill/fee progression, live cancel boundary behavior, and stale/exchange-backed fail-closed mutation; Web marks source labels and only exposes cancel for active open rows. | API Orders tests (`121/121`) and Web Orders tests (`3/3`) in `V1-ORDERS-LOCAL-PROOF-2026-05-11` | REQ-FUNC-012 | partially_verified | 2026-05-11 |
| QA-013 | simulation correctness/operator truth | Backtests expose deterministic owned simulation truth across run lifecycle, worker persistence, reports, timeline, replay parity, and Web details presentation. | API scopes runs/reports/timeline to the owner, persists explicit ranges and worker results, fails closed on empty symbol sets, preserves strategy/market/backtest/paper/live parity diagnostics, and returns pending report state without misleading 404s; Web renders create/list/details/table/report/timeline states through focused route/component/hook utilities. | API Backtests tests (`110/110`) and Web Backtests tests (`32/32`) in `V1-BACKTESTS-LOCAL-PROOF-2026-05-11` | REQ-FUNC-013 | partially_verified | 2026-05-11 |
| QA-014 | reporting/operator truth | Reports expose honest cross-mode and per-run performance summaries across loading, empty, success, and localized route states. | API aggregates BACKTEST and PAPER metrics deterministically; Web route shell renders the Reports surface, empty state does not imply hidden data, success state shows summary cards and tables, and route-reachable locale copy stays wired. | API Reports tests (`2/2`) and Web Reports tests (`5/5`) in `V1-REPORTS-LOCAL-PROOF-2026-05-11` | REQ-FUNC-014 | partially_verified | 2026-05-11 |
| QA-015 | auditability/operator truth | Logs/Audit Trail exposes owner-scoped audit events with trustworthy filters, bounded pagination, and non-executed metadata trace details. | API rejects unauthenticated requests, scopes rows to the owner, applies source/actor/severity filters and pagination bounds, and shows action-produced audit events; Web renders route shell, empty/loaded states, severity filter calls, and metadata JSON as text; production proof reads back an action-produced API-key probe audit event and renders `/dashboard/logs`. | API Logs tests (`5/5`) and Web Logs tests (`4/4`) in `V1-LOGS-AUDIT-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-fixture-action-proof-457bce05-2026-05-14.md` PASS; `history/plans/prod-ui-module-clickthrough-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-015 | verified | 2026-05-14 |
| QA-016 | exchange-boundary safety | Exchange Adapter preserves explicit capability truth and fails closed for unsupported exact operations while normalizing public market symbols consistently for downstream modules. | Gate.io public catalog emits canonical `BTCUSDT` style symbols from generic adapter ids; API-key probes, authenticated reads, live adapter, fee reconciliation, public reads, metadata, and symbol rules are covered locally; Web capability gating stays aligned with shared truth; production read-only/fail-closed proof verifies unsupported exchange probe rejection, Binance/Gate.io catalog reads, canonical Gate.io symbols, and authenticated readiness details. | API Exchange tests (`93/93`) and Web Exchanges/Profile API-key tests (`17/17`) in `V1-EXCHANGE-ADAPTER-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-security-exchange-proof-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-016 | verified | 2026-05-14 |
| QA-017 | reliability/observability | Worker health, readiness, runtime freshness, queue tuning, market-stream fanout, and job execution signals remain truthful and fail closed in local runtime contracts. | Protected worker endpoints reject unauthenticated access; split mode reports missing queues; inline mode skips passive freshness only without active demand; stale worker/market/session/decision signals fail; market-stream fanout retries after Redis startup failure; backtest worker jobs persist results; runtime-flow creates worker telemetry. | API Workers/stream/runtime tests (`88/88`) in `V1-WORKERS-LOCAL-PROOF-2026-05-11` | REQ-FUNC-017 | partially_verified | 2026-05-11 |
| QA-018 | security/privacy | Auth, ownership, secret handling, rate limiting, trusted-origin, ops-network, API-key privacy, and abuse throttling fail closed under Security/Privacy contracts. | Unauthenticated protected routes reject access; cross-user data is isolated; unsafe origins and disallowed ops networks are blocked; missing/malformed critical secrets make readiness not ready; raw API secrets stay encrypted/masked/redacted; rate-limit Redis degradation fails closed in production mode; profile password/account actions require valid credentials; stage-abuse flows are throttled; Web auth/profile states surface safe redirects and errors; production proof verifies protected route, ops diagnostics, metrics, API-key list redaction, untrusted Origin, no-store, and authenticated readiness behavior on deployed `2fc90a08`. | API Security/Privacy tests (`111/111`) and Web Auth/Profile tests (`48/48`) in `V1-SECURITY-PRIVACY-LOCAL-PROOF-2026-05-11`; `history/evidence/prod-security-exchange-proof-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-018 | verified | 2026-05-14 |
| QA-019 | usability/accessibility/responsiveness | Core public/dashboard routes render meaningful operator states across desktop and mobile with usable keyboard/focus and navigation behavior. | Public and authenticated dashboard routes render or redirect as expected; Dashboard and Wallets empty states explain next action; mobile menu opens with focus on the menu control and exposes primary navigation/account actions; shared UI state components cover loading/empty/error/success; no framework overlay appears; console/exception events are absent in the local browser proof; production route/module reachability passes for protected dashboard/admin routes on the deployed target; production desktop/mobile screenshots show rendered operator surfaces without horizontal overflow. | Route audit `PASS`; Web UX/a11y/state tests (`126/126`); Edge/CDP screenshots and console proof in `V1-UX-A11Y-MOBILE-LOCAL-PROOF-2026-05-11`; `V1-PRODUCTION-UI-CLICKTHROUGH-REFRESH-00169D7F-2026-05-13` production UI route audit PASS public `4`, dashboard `18`, admin `3`, legacy `3`; `history/plans/prod-ui-module-clickthrough-2fc90a08-2026-05-14.md` PASS; `history/evidence/prod-ux-a11y-mobile-proof-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-019 | verified | 2026-05-14 |
| QA-020 | security/usability/admin control | Admin subscription and user-management surfaces fail closed for non-admins while letting an admin inspect and safely manage role/plan state through coherent protected UI. | Unauthenticated and non-admin API requests are rejected; admin plan catalog and user list expose expected metadata; invalid entitlement payloads fail validation; self-demotion is blocked; admin subscription/user pages render protected HTML and visible tables without framework overlay; production admin route audits render `/admin/users` and `/admin/subscriptions` with authenticated admin auth. | API Subscriptions/Admin tests (`18/18`); focused subscriptions module tests (`2/2`); Web Admin/Profile Subscription tests (`7/7`); local protected admin route audit `PASS`; Edge/CDP screenshots in `V1-SUBSCRIPTIONS-ADMIN-LOCAL-PROOF-2026-05-12`; `history/plans/prod-ui-module-clickthrough-2fc90a08-2026-05-14.md` PASS | REQ-FUNC-020 | verified | 2026-05-14 |
| QA-021 | reliability/operations | Release-safety gates should fail closed unless rollback, smoke, freshness, SLO, alerts, sign-off, restore, and liveimport readback evidence exists for the target environment. | Public production health and fresh docs alone must not satisfy protected release gates. The gate must require target build-info, deploy smoke, protected runtime freshness, rollback proof/guard, RC/sign-off evidence, backup/restore proof, and LIVEIMPORT readback for the deployed candidate. | 2026-05-14 `V1-PROTECTED-OPS-GATE-457BCE05-2026-05-14`: production build-info is fresh for deployed `457bce05`, public API/Web smoke passes, protected runtime freshness passes, rollback proof passes with `shouldRollback=false` and no alerts, authenticated production UI clickthrough passes, controlled no-order-guard `LIVEIMPORT-03` passes for `TRXUSDT`, activation audit/plan are fresh, RC external gates/sign-off/checklist pass, production backup/restore drill passes through the VPS/Coolify Docker context, final preflight is `ready`, and the full non-dry-run release gate is `ready` with guardrails, typecheck, build, go-live smoke, deploy smoke, runtime freshness, and rollback guard all passing. | REQ-FUNC-021 | verified | 2026-05-14 |
| QA-022 | data lifecycle/capital safety | Deleted bots should not leave active-looking bot-owned runtime/trading rows that later block PAPER wallet reset, and wallet reset should fail closed when a currently active bot still uses that wallet. | Bot delete removes bot-owned positions, orders, trades, fills, signals, logs, runtime dedupe rows, sessions, events, stats, strategy links, market links, assistant config, and subagent config while preserving the strategy; PAPER reset returns `409` for an active wallet-linked bot and succeeds after deactivation when no open rows remain. | `history/tasks/v1-post-v1-wallet-bot-cleanup-hardening-2026-05-14-task.md`; `bots.delete-cleanup.e2e.test.ts` PASS (`1/1`); Wallets e2e PASS (`24/24`); API typecheck PASS | REQ-FUNC-025 | verified | 2026-05-14 |
| QA-023 | usability/operator trust | Common crypto symbols shown in dashboard/runtime tables should not degrade to generic placeholders during a CoinGecko outage when a verified curated icon exists. | For the common trading basket, API icon lookup returns `source=curated`, `placeholder=false`, and an asset-specific icon URL when upstream responses are `503`; unknown symbols still use the deterministic placeholder. | `history/tasks/post-v1-crypto-icon-consistency-2026-05-14-task.md`; `icons.e2e.test.ts` PASS (`6/6`) | REQ-FUNC-026 | verified | 2026-05-14 |
| QA-024 | simulation auditability/data lifecycle | Historical backtest results should keep the exact strategy and market-universe context used at run creation even when mutable strategy or universe records are later edited, and deletes should fail closed while history still references the source record. | New runs persist `seedConfig.contextSnapshot`; list/timeline/replay use snapshot strategy truth before mutable strategy records; deleting a referenced strategy or market universe returns `409`. | `history/tasks/post-v1-strategy-snapshot-history-2026-05-14-task.md`; focused API e2e PASS (`44/44`) across backtests, strategies, and markets | REQ-FUNC-027 | verified | 2026-05-14 |
| QA-025 | security/commercial readiness | Security-critical paths should fail closed under adversarial review before any commercial security claim: role changes invalidate stale privilege, weak deploy secrets are rejected, logs and UI errors do not expose sensitive internals, LIVE-sensitive execution checks current entitlement, exchange adapters preserve derivative safety parameters, unknown LIVE statuses do not become filled truth, and production dependencies have no known audit vulnerabilities. | Second-round security agents report completed findings; production audit reports no known vulnerabilities; focused API/Web security packs pass; build/typecheck/guardrails pass; residual production/pentest/LIVE mutation gates remain explicit. | `history/tasks/security-red-team-hardening-2026-05-21-task.md`; `pnpm audit --prod`; focused API/Web regression packs; API/Web typecheck; build; guardrails | REQ-SEC-038 | verified | 2026-05-21 |
| QA-026 | security/ops hardening | Residual local hardening should prevent secret leakage in release rehearsal artifacts, avoid client-side protected data loads before auth confirmation, withhold admin content until client role confirmation, keep API-key response state secret-free, fail closed on LIVE entitlement downgrade, avoid root runtime containers, align VPS templates with hardened Redis/secret defaults, emit HSTS, and keep local datastores bound to localhost. | Secret-bearing rehearsal flags are rejected; generated command strings do not contain secret values; frontend tests prove disabled runtime loading and admin role gate; API tests prove downgrade `403` behavior and runtime topology exclusion; guardrails reject root runtime Dockerfiles; VPS compose config parses with hardened template; build/typecheck pass; cleanup checks pass. | `history/tasks/security-red-team-hardening-2026-05-21-task.md`; `scripts/runV1StageRehearsal.test.mjs`; `scripts/repoGuardrails.test.mjs`; focused API/Web tests; build; guardrails | REQ-SEC-039 | verified | 2026-05-21 |

Scenario shape:

`stimulus -> environment -> artifact -> response -> measure -> verification`

## Scenario Notes

- 2026-05-21 `SECURITY-RED-TEAM-HARDENING-2026-05-21` updates security,
  privacy, fail-closed trading safety, deploy hygiene, and commercial
  readiness posture. Local hardening is verified by agent reports, dependency
  audit, focused API/Web regression packs, typecheck, build, and guardrails.
  This does not replace protected production proof, LIVE exchange-side mutation
  proof, or an independent penetration/VPS configuration review.
- 2026-05-21 security continuation updates frontend, backend entitlement, and
  ops hardening. The remaining local P2 queue from the first security report is
  closed with tests and guardrails; remaining risk is external/protected
  evidence rather than a known local P1/P2 code defect.
- 2026-05-21 `LOCAL-CERTAINTY-CLOSURE-2026-05-21` updates reporting
  reliability, i18n, UX consistency, migration confidence, and release-local
  validation: Reports now use immutable execution-mode snapshots for new
  trades, route-reachable i18n audit passed with `0` findings, shared modal and
  shared state components replaced remaining local UI inconsistencies, Prisma
  migration replay/status/validation passed, full API/Web tests passed, build
  passed, and go-live smoke passed. Protected production readiness still
  depends on `AUD-19`.
- 2026-05-20 `V1-PROTECTED-PREFLIGHT-DD1A1FAF-2026-05-20` updates `QA-021`:
  the release gate correctly fails closed for the current evidence date. Public
  build-info and public smoke pass for deployed `dd1a1faf79f8ac...`, but
  missing protected input names and stale protected evidence keep final V1
  readiness blocked. The current no-secret operator handoff is
  `history/releases/v1-operator-unblock-packet-dd1a1faf-2026-05-20.md`, and
  `ops:operator-unblock:check` now validates it.
- 2026-05-20 `V1-OPERATOR-UNBLOCK-TOOLING-INDEX-SYNC-2026-05-20` updates
  `QA-021`: the current no-secret operator packet validation is now included
  in reusable audit tooling and `audit:manifest:verify`, so future release
  evidence reruns fail sooner if the protected handoff drifts.
- 2026-05-20 `V1-AGENT-BLOCKER-SWEEP-DD1A1FAF-2026-05-20` updates
  `QA-021`: parallel agents confirmed the release gate is correctly fail-closed
  and cannot be advanced by public smoke, build-info, or more non-secret prep.
- 2026-05-24 `ARCH-GRAPH-API-SUPPORT-ROUTES-BACKFILL-2026-05-24` adds
  `QAS-DOC-022`: route traceability improves because all inventoried API route
  files are now referenced by graph CSV records. Proof: graph generation passed
  with `461` nodes, `559` relations, and `20` chains; drift audit passed with
  `apiRoutes` at `22/22`, total coverage `425/796`, and `371` remaining gaps.
- 2026-05-24 `ARCH-GRAPH-RUNTIME-SUPPORT-SERVICES-BACKFILL-2026-05-24` adds
  `QAS-DOC-023`: runtime maintainability improves because bot/runtime/engine
  support services are now visible in the graph instead of only routes and
  controllers. Proof: graph generation passed with `500` nodes, `577`
  relations, and `21` chains; drift audit passed with total coverage `466/796`
  and `330` remaining gaps.
- 2026-05-24 `ARCH-GRAPH-API-PLATFORM-SAFETY-BACKFILL-2026-05-24` adds
  `QAS-DOC-024`: API platform maintainability and safety traceability improve
  because runtime config, middleware guards, shared errors/logging/symbol
  libraries, and their tests are now graph-visible. Proof: graph generation
  passed with `520` nodes, `597` relations, and `22` chains; drift audit passed
  with total coverage `478/796` and `318` remaining gaps.
- 2026-05-24 `ARCH-GRAPH-WEB-RUNTIME-SURFACES-BACKFILL-2026-05-24` adds
  `QAS-DOC-025`: Web runtime maintainability improves because Dashboard Home
  runtime sections and bot monitoring surfaces are now graph-visible from UI
  components through Web helpers, runtime API service boundaries, tests, and
  docs. Proof: graph generation passed with `543` nodes, `624` relations, and
  `23` chains; drift audit passed with total coverage `510/796` and `286`
  remaining gaps.
- 2026-05-24 `ARCH-GRAPH-AUTH-SESSION-DEEP-BACKFILL-2026-05-24` adds
  `QAS-DOC-026`: Auth maintainability improves because public auth pages, Web
  forms/hooks/service/AuthContext, API auth routes/controller/service/session
  helpers, User model, tests, and docs are graph-visible as one chain. Proof:
  graph generation passed with `573` nodes, `659` relations, and `24` chains;
  drift audit passed with total coverage `534/796` and `262` remaining gaps.
- 2026-05-24 `ARCH-GRAPH-FULL-DRIFT-CLOSURE-2026-05-24` adds `QAS-DOC-027`:
  graph maintainability improves because the representative source/test/docs/
  config/pipeline inventory is now fully path-visible from CSV records. Proof:
  graph generation passed with `635` nodes, `781` relations, and `26` chains;
  drift audit passed with total coverage `796/796` and `0` remaining gaps.
- 2026-05-24 `ARCH-GRAPH-STRICT-GUARDRAIL-2026-05-24` adds `QAS-DOC-028`:
  graph maintainability improves because zero-drift traceability is now
  enforced by `pnpm run quality:guardrails`, not only reported by a standalone
  audit. Proof: strict drift passed with `796/796` covered and `0` missing;
  guardrail unit tests passed `9/9`; `quality:guardrails` passed and reported
  `Architecture graph drift: OK`.
