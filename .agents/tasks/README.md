# Agent Tasks

Use this folder for agent-local task contracts only when the task does not
belong in `docs/planning/`.

Default rule: repository execution tasks should still use
`.codex/templates/task-template.md` and live in `docs/planning/` when they
represent canonical delivery history.
