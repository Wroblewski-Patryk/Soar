# Source Directory

Use this directory for implementation in simple projects. Framework projects may use their native directories such as `app/`, `apps/`, `backend/`, `web/`, or `mobile/`.

When a project uses framework-specific directories, document the mapping in `architecture/modules.md`.
