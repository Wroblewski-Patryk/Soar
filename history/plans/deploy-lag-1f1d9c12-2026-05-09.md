# Deploy Lag Evidence - 1f1d9c12 (2026-05-09)

## Status
- Result: **BLOCKED**
- Environment: production
- Pushed SHA: `1f1d9c12e0cc99884eced81546802a261b0925e9`
- Latest pushed handoff SHA:
  `d355df93107f4d7ff9d6231107528295cbc873c2`
- Latest pushed source batch SHA:
  `010b4f8b6abfaf4c24d26550eb4761215d119f21`
- Last observed production SHA:
  `010b4f8b6abfaf4c24d26550eb4761215d119f21`
- Web base URL: `https://soar.luckysparrow.ch`
- Evidence date: 2026-05-09

## Build Info Wait

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 1f1d9c12 --timeout-seconds 900 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
...
[wait:web-build-info] attempt=60 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose 1f1d9c12; lastSeenSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d lastError=n/a
```

## Follow-Up Wait

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 1f1d9c12 --timeout-seconds 300 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
...
[wait:web-build-info] attempt=20 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose 1f1d9c12; lastSeenSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d lastError=n/a
```

## Second Follow-Up Wait

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 1f1d9c12 --timeout-seconds 300 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
...
[wait:web-build-info] attempt=20 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose 1f1d9c12; lastSeenSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d lastError=n/a
```

## Third Follow-Up Wait

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 1f1d9c12 --timeout-seconds 180 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
...
[wait:web-build-info] attempt=12 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=1f1d9c12
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose 1f1d9c12; lastSeenSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d lastError=n/a
```

## Latest Handoff Follow-Up Wait

After the deploy-lag/operator handoff evidence was committed and pushed as
`d355df93107f4d7ff9d6231107528295cbc873c2`, production build-info was checked
again with a bounded 120-second wait.

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha d355df93107f4d7ff9d6231107528295cbc873c2 --timeout-seconds 120 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=d355df93107f4d7ff9d6231107528295cbc873c2
...
[wait:web-build-info] attempt=8 status=200 gitSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d expected=d355df93107f4d7ff9d6231107528295cbc873c2
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose d355df93107f4d7ff9d6231107528295cbc873c2; lastSeenSha=c50e1e7cf1e37d9c799031cacbb30a834f57e81d lastError=n/a
```

## Interpretation

The two-commit docs/evidence batch ending at `1f1d9c12` is pushed to
`origin/main`, but production build-info did not expose it within the accepted
900-second wait window, two additional 300-second follow-up waits, or the
third 180-second follow-up wait.

The follow-up source-of-truth handoff commit `d355df93` is also pushed to
`origin/main`, but production build-info still remained on `c50e1e7c` during
the bounded 120-second follow-up wait.

## Gate.io Source Batch Follow-Up

The later three-commit Gate.io source batch ending at
`010b4f8b6abfaf4c24d26550eb4761215d119f21` was pushed to `origin/main`.
Production build-info advanced to `d355df93`, closing the prior handoff lag.
An initial follow-up wait used an incorrect full SHA for short commit
`010b4f8b`, which made the already-deployed source batch look stale.

Command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 010b4f8bb55081bc3cccb9f27669dbf9a312f62d --timeout-seconds 180 --interval-seconds 15
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=d355df93107f4d7ff9d6231107528295cbc873c2 expected=010b4f8bb55081bc3cccb9f27669dbf9a312f62d
...
[wait:web-build-info] attempt=12 status=200 gitSha=d355df93107f4d7ff9d6231107528295cbc873c2 expected=010b4f8bb55081bc3cccb9f27669dbf9a312f62d
[wait:web-build-info] failed: Timed out waiting for https://soar.luckysparrow.ch/api/build-info to expose 010b4f8bb55081bc3cccb9f27669dbf9a312f62d; lastSeenSha=d355df93107f4d7ff9d6231107528295cbc873c2 lastError=n/a
```

Corrected command:

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 010b4f8b6abfaf4c24d26550eb4761215d119f21 --timeout-seconds 30 --interval-seconds 5
```

Result:

```text
[wait:web-build-info] attempt=1 status=200 gitSha=010b4f8b6abfaf4c24d26550eb4761215d119f21 expected=010b4f8b6abfaf4c24d26550eb4761215d119f21
[wait:web-build-info] PASS
```

Current production public smoke for `010b4f8b` passed for API `/health`, API
`/ready`, and Web `/`. No-secret final V1 preflight also reports build-info
and public smoke PASS while keeping protected/formal V1 blockers explicit.

Current production truth is now `010b4f8b` by Web build-info evidence.
Protected evidence, authenticated UI audit, and final release
commands must keep using the build-info-proven SHA, not merely pushed
`origin/main`.

## Pushed Batch Scope Audit

Command:

```powershell
git diff --name-only c50e1e7c..1f1d9c12
git diff --name-only c50e1e7c..1f1d9c12 -- apps packages prisma scripts
```

Result:
- The pushed but not deployed batch changes repository state and evidence
  files under `.agents/`, `.codex/`, and `docs/`.
- The runtime-code path check for `apps`, `packages`, `prisma`, and `scripts`
  returned no files.

Interpretation:
- The deploy lag affects operator/source-of-truth evidence availability on
  production build-info, not a newer runtime code path.
- Protected runtime readback may continue to target build-info-proven
  `c50e1e7c` unless the operator specifically needs the newer docs/evidence
  batch deployed before running the protected pack.
- Final release evidence must still use build-info-proven SHA values and must
  not claim `1f1d9c12` is deployed until `/api/build-info` proves it.

## Operator Access Check

Names-only deploy variable scan:

```text
COOLIFY_PROD_DEPLOY_HOOK_URL=MISSING
COOLIFY_PROD_ROLLBACK_HOOK_URL=MISSING
COOLIFY_API_TOKEN=MISSING
COOLIFY_URL=MISSING
PROD_WEB_BASE_URL=MISSING
PROD_API_BASE_URL=MISSING
```

Read-only SSH/VPS inspection was attempted but did not authenticate from the
current workstation context. No container state was changed and no deployment
hook was called.

## Current Production Public Smoke

Command:

```powershell
node scripts\deploySmokeCheck.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch --no-workers
```

Result:

```text
[deploy-smoke] summary
- PASS API /health -> 200
- PASS API /ready -> 200
- PASS WEB / -> 200
[deploy-smoke] all checks passed
```

This confirmed the previously deployed `c50e1e7c` public API/Web surface was
healthy while `1f1d9c12` was still not deployed. A later public smoke for the
currently deployed `d355df93` surface also passed for API `/health`, API
`/ready`, and Web `/`.

## Next Action

The pushed Gate.io source batch is deployed and public production smoke passes.
The current latest pushed source-of-truth handoff SHA is
`010b4f8b6abfaf4c24d26550eb4761215d119f21`; the prior handoff
`d355df93107f4d7ff9d6231107528295cbc873c2` is historical. Before V1 release,
complete protected evidence, authenticated UI audit, rollback proof, DB restore
context, and formal release checklist gates.

## Operator Handoff

### Required Operator Context
- Access to the production Coolify project/resource that deploys
  `https://soar.luckysparrow.ch`.
- Ability to inspect the deployment queue/logs for the Web application.
- Ability to trigger the approved production redeploy path or provide a
  deploy webhook/API token outside repository artifacts.

### Decision Tree
1. If Coolify already has a queued or running deployment for `010b4f8b`, wait
   for it to finish and then run the verification commands below.
2. If Coolify has no queued/running deployment for `010b4f8b`, trigger the
   approved production deploy for the Web application from the dashboard or
   approved secret-managed webhook.
3. If Coolify reports a failed deployment, capture the deploy log and do not
   create an empty retrigger commit. Fix the logged blocker or rerun the
   approved deploy path after operator review.
4. If build-info still reports `d355df93` after a successful Coolify deploy,
   inspect build metadata wiring for `SOURCE_COMMIT`,
   `COOLIFY_GIT_COMMIT_SHA`, and the Web runtime environment before running
   protected V1 evidence.

### Verification Commands

```powershell
node scripts\waitForWebBuildInfo.mjs --web-base-url https://soar.luckysparrow.ch --expected-sha 010b4f8b6abfaf4c24d26550eb4761215d119f21 --timeout-seconds 900 --interval-seconds 15
node scripts\deploySmokeCheck.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch --no-workers
node scripts\runV1FinalPreflight.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch --expected-sha 010b4f8b6abfaf4c24d26550eb4761215d119f21 --today 2026-05-09 --json-output history\artifacts\_artifacts-v1-final-preflight-010b4f8b-2026-05-09.json --markdown-output history\releases\v1-final-preflight-010b4f8b-2026-05-09.md
```

### Acceptance Criteria
- Production Web build-info reports
  `010b4f8b6abfaf4c24d26550eb4761215d119f21`.
- Public API/Web smoke passes.
- No-secret final preflight reports build-info and public smoke PASS while
  keeping protected V1 evidence blockers explicit.
- Protected `LIVEIMPORT-03`, rollback proof, restore evidence, RC approval,
  and authenticated/admin UI clickthrough are not claimed unless their
  separate protected artifacts exist.

### Non-Accepted Evidence
- `git push` success.
- Public `/health`, `/ready`, or Web `/` without matching build-info.
- Local `HEAD`.
- Empty retrigger commits that do not correspond to an inspected Coolify
  queue/log state.
