# LUC-2731 Post-Deploy Runtime Freshness Missing-Test Links

## Header
- ID: LUC-2731
- Title: [Soar][Architecture QA][LUC-2730] Cover post-deploy runtime freshness missing-test links
- Task Type: fix
- Current Stage: verification
- Status: DONE
- Owner: QA/Test
- Priority: P0
- Module Confidence Rows: Architecture Evidence Graph relation confidence; post-deploy runtime freshness tooling
- Requirement Rows: runtime/deploy freshness proof traceability
- Mission ID: LUC-2731-POST-DEPLOY-RUNTIME-FRESHNESS-MISSING-TEST-LINKS-2026-06-07
- Mission Status: VERIFIED

## Context
[LUC-2730](/LUC/issues/LUC-2730) refreshed architecture-awareness after
completed [LUC-2725](/LUC/issues/LUC-2725) and identified
`scripts/checkPostDeployRuntimeFreshness.mjs` as the current top actionable
missing-test family. The missing function-level anchors were `fetchWithTimeout`
and `main`.

## Goal
Add focused local proof and scanner-readable architecture relation rows for the
post-deploy runtime freshness helper without running production smoke or
changing deploy/runtime behavior.

## Constraints
- Preserve secret handling: secret-bearing CLI flags remain rejected.
- Do not print token, password, cookie, or private header values in tests or
evidence.
- Do not deploy, push, restart, rollback, mutate env, access accounts, run
protected production smoke, touch exchange/database state, or mutate
live-trading settings.

## Definition of Done
- [x] `fetchWithTimeout` has focused timeout/abort proof.
- [x] `main` has focused local endpoint PASS and fail-closed proof.
- [x] Direct `LUC-2731` relation rows exist in
      `docs/architecture/relations/priority-test-links.csv`.
- [x] Syntax, focused test, relation readback, architecture graph generation,
      and guardrails pass.
- [x] Evidence and project state are updated.

## Forbidden
- Product runtime behavior changes beyond import safety and test seams.
- Workarounds, duplicate deploy-smoke logic, or a parallel freshness checker.
- Production, protected, account, secret, exchange, database, deploy, restart,
  rollback, or live-trading mutation.

## Implementation Plan
1. Make `scripts/checkPostDeployRuntimeFreshness.mjs` import-safe while
   preserving direct CLI execution.
2. Export existing helpers and add dependency injection for args, env, console,
   help exit behavior, and fetch implementation.
3. Add `scripts/checkPostDeployRuntimeFreshness.test.mjs` with a local HTTP
   server fixture covering secret CLI rejection, timeout abort, PASS output
   without secret leakage, and fail-closed HTTP behavior.
4. Add direct `LUC-2731` relation rows for `fetchWithTimeout`, `main`, and
   `parseArgs`.
5. Run focused checks, graph generation, and guardrails.

## Acceptance Criteria
- `scripts/checkPostDeployRuntimeFreshness.mjs#fetchWithTimeout` and
  `scripts/checkPostDeployRuntimeFreshness.mjs#main` are directly linked to a
  focused test file.
- The production CLI entrypoint still runs only when invoked directly.
- Tests prove local behavior without real production auth, protected smoke, or
  secret-bearing argv values.

## Validation Evidence
- `node --check scripts/checkPostDeployRuntimeFreshness.mjs`: PASS.
- `node --check scripts/checkPostDeployRuntimeFreshness.test.mjs`: PASS.
- `node --test scripts/checkPostDeployRuntimeFreshness.test.mjs`: PASS (`4/4`).
- `rg -n "LUC-2731" docs/architecture/relations/priority-test-links.csv`:
  PASS (`3` rows).
- `pnpm run architecture:graph:generate`: PASS (`653` nodes / `842`
  relations / `27` chains).
- `pnpm run quality:guardrails`: PASS.

## Architecture Evidence
- Architecture source reviewed:
  `docs/status/architecture-awareness-report.md`,
  `docs/architecture/relations/priority-test-links.csv`,
  `scripts/checkPostDeployRuntimeFreshness.mjs`,
  `apps/api/src/router/workers-runtime-freshness.test.ts`.
- Affected architecture entities:
  - `feature:checkPostDeployRuntimeFreshness.mjs`
  - `function:fetchWithTimeout`
  - `function:main`
  - `function:parseArgs`
- Fits approved architecture: yes.
- Mismatch discovered: no.
- Decision required from user: no.
- Follow-up architecture doc updates: graph/status outputs regenerated by
  `pnpm run architecture:graph:generate`.

## Deployment / Ops Evidence
- Deploy impact: none.
- Env or secret changes: none.
- Health-check impact: none.
- Smoke steps updated: no; local helper proof only.
- Rollback note: revert the script import-safety/test-seam patch and relation
  rows if needed.
- Observability or alerting impact: none.

## Autonomous Loop Evidence

### 1. Analyze Current State
- Issues: architecture-awareness report listed
  `scripts/checkPostDeployRuntimeFreshness.mjs#fetchWithTimeout` and `#main`
  as missing direct test links.
- Gaps: existing endpoint tests covered API behavior, but not the post-deploy
  CLI helper entrypoint and timeout wrapper directly.
- Architecture constraints: use scanner-readable relation rows and existing
  Node test style.

### 2. Select One Priority Mission Objective
- Selected task: cover LUC-2731 runtime freshness helper missing-test links.
- Priority rationale: critical Paperclip assignment and current top actionable
  family from parent TSA refresh.

### 3. Plan Implementation
- Files or surfaces to modify:
  `scripts/checkPostDeployRuntimeFreshness.mjs`,
  `scripts/checkPostDeployRuntimeFreshness.test.mjs`,
  `docs/architecture/relations/priority-test-links.csv`, generated graph/status
  outputs, and local state/evidence docs.
- Edge cases: secret-bearing argv rejection, timeout abort, PASS output, HTTP
  fail-closed handling.

### 4. Execute Implementation
- Implementation notes: kept CLI behavior behind direct invocation guard,
  exported current helpers, and added injectable test seams.

### 5. Verify and Test
- Validation performed: syntax checks, focused Node test, relation readback,
  architecture graph generation, and guardrails.
- Result: verified.

### 6. Self-Review
- Simpler option considered: relation rows only against existing endpoint tests.
  Rejected because the missing anchors were script-level helpers and `main`.
- Technical debt introduced: no.
- Refinements made: test captures console output and asserts token value is not
  printed.

### 7. Update Documentation and Knowledge
- Docs updated: this task evidence file and local state/context files.
- Context updated: yes.
- Learning journal updated: not applicable; no new recurring pitfall confirmed.

## Result Report
- Task summary: added focused local proof for the post-deploy runtime freshness
  helper and linked function anchors to scanner-readable relation rows.
- Files changed:
  - `scripts/checkPostDeployRuntimeFreshness.mjs`
  - `scripts/checkPostDeployRuntimeFreshness.test.mjs`
  - `docs/architecture/relations/priority-test-links.csv`
  - generated architecture graph/status outputs from
    `pnpm run architecture:graph:generate`
  - local state/evidence files for this LUC-2731 closure
- What is incomplete: none for this issue.
- Residual risk: this proves the local helper contract only; it intentionally
  does not prove a live production `/workers/runtime-freshness` response.
- Next owner: parent TSA/PM lane may refresh architecture-awareness after
  closure and choose the next non-duplicate missing-test family.
