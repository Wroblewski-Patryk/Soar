# LUC-5360 API Health Latency Correlation

## Status

- Result: **DONE / PARTIALLY_VERIFIED / ACTIVE_API_TAIL_NOT_REPRODUCED / TLS_PROXY_VARIANCE_CLASSIFIED**
- Issue: [LUC-5360](/LUC/issues/LUC-5360)
- Parent: [LUC-5356](/LUC/issues/LUC-5356)
- Evidence window: 2026-06-20T23:59Z to 2026-06-21T00:00Z
- Generated by: 09 DRE (Deployment & Reliability Engineer)

## Scope

Read-only DRE/Ops correlation for recurring production API `/health` latency
tails.

Included:

- public production smoke for API `/health`, API `/ready`, Web `/`, and Web
  `/api/build-info`
- bounded public `curl.exe` timing with phase breakdown for API `/health`,
  API `/ready`, and Web `/`
- production Web build-info readback
- read-only Coolify API projection with redacted, aggregate-only output

Excluded:

- deploy, push, restart, rollback, env edit, secret/account readback,
  database/Redis mutation, raw log capture, protected account/trading/payment
  action, and live-trading action

## Prior Signals Consumed

[LUC-5356](/LUC/issues/LUC-5356) reproduced API `/health` latency tails:

- first five-sample timing: API `/health` max `7141 ms`, average `2412.8 ms`,
  all `200`
- focused API `/health` recheck: `20/20` `200`, max `2729 ms`, average
  `486.4 ms`, four samples over `1000 ms`
- Web `/` normalized on focused recheck: `10/10` `200`, max `213 ms`

Earlier [LUC-5252](/LUC/issues/LUC-5252) classified similar API `/health` and
`/ready` low-second tails as concentrated in TLS/appconnect/start-transfer
timing, with `/health` stronger than `/ready`.

## Public Smoke

Command:

```powershell
pnpm run -s ops:deploy:smoke -- --base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch --no-workers
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`

## Build Info

Production Web `/api/build-info` returned:

- `gitSha`: `42177530f2a2ddc22832133b545bccab6ab404eb`
- `gitRef`: `main`
- `buildId`: `Urnq8xtZUh932c0e3vKGl`
- `metadataGeneratedAt`: `2026-06-15T21:00:54.489Z`
- `metadataSource`: `env-runtime`
- `checkedAt`: `2026-06-21T00:00:13.3784566Z`

`env-runtime` remains diagnostic-only provenance and is not release-grade build
metadata.

## Phase Timing Recheck

Command shape:

```powershell
curl.exe -sS -L --max-time 20 -o NUL -w "%{http_code} %{time_namelookup} %{time_connect} %{time_appconnect} %{time_pretransfer} %{time_starttransfer} %{time_total} %{remote_ip}\n" <url>
```

Summary:

| Target | Statuses | Samples | Max ms | Avg ms | >1000 ms | Max start-transfer ms | Remote IP |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| API `/health` | `200:30` | 30 | 585.7 | 130.9 | 0 | 585.5 | `141.227.149.67` |
| API `/ready` | `200:20` | 20 | 205.9 | 106.6 | 0 | 205.8 | `141.227.149.67` |
| Web `/` | `200:10` | 10 | 1633.5 | 288.5 | 1 | 1598.8 | `141.227.149.67` |

Slow samples over one second:

| Target | Sample | Checked at UTC | Status | DNS ms | Connect ms | TLS/appconnect ms | Start-transfer ms | Total ms |
| --- | ---: | --- | --- | ---: | ---: | ---: | ---: | ---: |
| Web `/` | 10 | 2026-06-20T23:59:30.6883217Z | `200` | 10.7 | 42.4 | 1573.6 | 1598.8 | 1633.5 |

Interpretation:

- The active API `/health` tail did not reproduce in this bounded window.
- API `/health` and `/ready` returned only `200` responses and stayed below
  one second.
- The only current slow sample was Web `/`, and nearly all of that sample was
  in TLS/appconnect/pretransfer before application response.
- This aligns with the earlier low-second tail classification as intermittent
  edge/proxy/network/TLS variance more than API handler or database-readiness
  behavior.

## Coolify / VPS Read-Only Projection

Secret handling:

- Present binding names were checked without values:
  `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_TOKEN`,
  `COOLIFY_SOAR_PROJECT_ID`, `COOLIFY_SOAR_PRODUCTION_ENVIRONMENT`,
  `COOLIFY_SOAR_API_APP_ID`, `COOLIFY_SOAR_WEB_APP_ID`,
  `COOLIFY_SOAR_POSTGRES_RESOURCE_ID`, `COOLIFY_SOAR_REDIS_RESOURCE_ID`, and
  `VPS_HOST`.
- No token values, raw resource ids, internal URLs, cookies, credentials,
  database values, raw deployment objects, or log bodies were printed or stored.

Authenticated Coolify `GET` projection:

| Check | Result | Timing |
| --- | --- | ---: |
| `GET /api/v1/version` | pass | 164 ms |
| `GET /api/v1/teams/current` | pass | 79 ms |
| `GET /api/v1/projects/{configured-project-id}` | pass | 45 ms |
| `GET /api/v1/projects/{configured-project-id}/environments` | pass | 53 ms |
| `GET /api/v1/projects/{configured-project-id}/{configured-production-environment}` | pass | 152 ms |
| `GET /api/v1/resources` | pass | 214 ms |
| `GET /api/v1/deployments` | pass | 47 ms |

Production environment projection:

- applications: `6`
- application names:
  `workers-backtest`, `soar-web`, `workers-market-stream`,
  `workers-execution`, `workers-market-data`, `soar-api`
- application statuses: `running:unknown` for all six rows
- databases: `1`
- visible deployments: `0`

The projection proves read-only Coolify reachability and no active visible
deployment queue, but does not expose enough application-status fidelity to
prove or disprove container pressure.

## Classification

Current classification:

- active outage: not present
- active API `/health` low-second tail: not reproduced in this window
- strongest cross-run correlation: TLS/appconnect/start-transfer variance
- DRE/Ops root-cause confidence: partial
- Core Backend handoff: not created, because the current API timing normalized
  and the slow sample was TLS/appconnect-bound

If API `/health` tails recur, the next smallest DRE/Ops action is a same-window
host/proxy/container-pressure capture plus sanitized API/proxy log-window
summary. Backend hardening should be routed only if a recurrence shows
application start-transfer delay after TLS/connect are normal.

## Disposition

[LUC-5360](/LUC/issues/LUC-5360) can close as a read-only DRE correlation
checkpoint:

- public production reachability is healthy
- the delegated API `/health` tail did not reproduce in the current 30-sample
  check
- prior and current slow samples point more strongly to intermittent
  edge/proxy/network/TLS variance than API handler logic
- no additional child issue is warranted until a fresh recurrence supplies
  same-window host/proxy/container or application-delay evidence

