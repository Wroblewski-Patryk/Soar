# LUC-5387 Production Performance And Server Health Watch

## Status

- Result: **DONE / PARTIALLY_VERIFIED / APP_REACHABLE / API_HEALTH_TLS_TAIL_WATCH**
- Issue: [LUC-5387](/LUC/issues/LUC-5387)
- Evidence window: 2026-06-21T00:22Z to 2026-06-21T00:25Z
- Generated by: 09 DRE (Deployment & Reliability Engineer)

## Scope

Read-only recurring production performance and server-health watch for Soar.

Included:

- public production smoke for API `/health`, API `/ready`, Web `/`, Web
  `/api/build-info`, and protected API `/workers/ready`
- bounded public `curl.exe` phase timing for API `/health`, API `/ready`, and
  Web `/`
- production Web build-info readback
- authenticated production UI module clickthrough audit
- read-only Coolify project/environment/resource projection with redacted,
  aggregate-only output
- Coolify stack env checker unit proof

Excluded:

- deploy, push, restart, rollback, environment edit, secret/account readback,
  database/Redis mutation, raw log capture, screenshot capture, production
  account mutation, exchange action, order, position, payment/subscription
  mutation, or live-trading action

## Wake Context

The wake payload assigned [LUC-5387](/LUC/issues/LUC-5387) with no pending
comments and `fallbackFetchNeeded=false`. Checkout was already claimed by the
harness, so this heartbeat used the inline issue scope and did not call
checkout again.

## Smoke

First command:

```powershell
pnpm run -s ops:deploy:smoke -- --base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`
- FAIL API `/workers/ready` -> `401`

Interpretation: the pre-bound `SMOKE_AUTH_TOKEN` path is stale or not accepted
for protected workers readiness in this runner.

Second command with `SMOKE_AUTH_TOKEN` suppressed for the child process so the
script uses env-bound email/password login:

```powershell
pnpm run -s ops:deploy:smoke -- --base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`
- PASS API `/workers/ready` -> `200`

## Build Info

Production Web `/api/build-info` returned:

- `gitSha`: `42177530f2a2ddc22832133b545bccab6ab404eb`
- `gitRef`: `main`
- `buildId`: `Urnq8xtZUh932c0e3vKGl`
- `metadataGeneratedAt`: `2026-06-15T21:00:54.489Z`
- `metadataSource`: `env-runtime`
- `checkedAt`: `2026-06-21T00:23:12.6976028Z`

`env-runtime` remains diagnostic-only provenance and is not release-grade build
metadata. Release provenance remains routed through [LUC-4912](/LUC/issues/LUC-4912).

## Phase Timing

| Target | Statuses | Samples | Max ms | Avg ms | >1000 ms | Max start-transfer ms | Remote IP |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| API `/health` | `200:20` | 20 | 1515.0 | 281.1 | 2 | 1514.9 | `141.227.149.67` |
| API `/ready` | `200:10` | 10 | 196.7 | 122.2 | 0 | 196.6 | `141.227.149.67` |
| Web `/` | `200:10` | 10 | 257.7 | 189.2 | 0 | 211.9 | `141.227.149.67` |

Slow samples over one second:

| Target | Sample | Checked at UTC | Status | DNS ms | Connect ms | TLS/appconnect ms | Start-transfer ms | Total ms |
| --- | ---: | --- | --- | ---: | ---: | ---: | ---: | ---: |
| API `/health` | 9 | 2026-06-21T00:22:36.1384190Z | `200` | 11.0 | 27.8 | 1059.6 | 1081.1 | 1081.1 |
| API `/health` | 10 | 2026-06-21T00:22:37.5180355Z | `200` | 6.3 | 28.8 | 1335.8 | 1514.9 | 1515.0 |

Interpretation:

- No outage or HTTP failure was observed.
- API `/ready` and Web `/` were responsive in this window.
- API `/health` reproduced two low-second tails dominated by
  TLS/appconnect/start-transfer timing.
- This supports continued DRE watchfulness but does not justify a new Backend
  repair child without same-window host/proxy/container pressure or app log
  evidence.

## Authenticated UI Clickthrough

Command:

```powershell
node scripts/runProdUiModuleClickthroughAudit.mjs --web-base-url https://soar.luckysparrow.ch --api-base-url https://api.soar.luckysparrow.ch --expected-sha 42177530f2a2ddc22832133b545bccab6ab404eb --output-json history/artifacts/luc-5387-prod-ui-module-clickthrough-2026-06-21.json --output-md history/evidence/luc-5387-prod-ui-module-clickthrough-2026-06-21.md --today 2026-06-21
```

Result: PASS.

- public routes: PASS `4/4`
- dashboard routes: PASS `18/18`
- admin routes: PASS `3/3`
- legacy redirects: PASS `3/3`
- dashboard auth: login present
- admin auth: login present

Evidence:

- `history/evidence/luc-5387-prod-ui-module-clickthrough-2026-06-21.md`
- `history/artifacts/luc-5387-prod-ui-module-clickthrough-2026-06-21.json`

## Coolify / VPS Read-Only Projection

Secret handling:

- Present binding names were checked without values:
  `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_TOKEN`,
  `COOLIFY_SOAR_PROJECT_ID`, `COOLIFY_SOAR_PRODUCTION_ENVIRONMENT`,
  `COOLIFY_SOAR_API_APP_ID`, `COOLIFY_SOAR_WEB_APP_ID`,
  `COOLIFY_SOAR_POSTGRES_RESOURCE_ID`, `COOLIFY_SOAR_REDIS_RESOURCE_ID`, and
  `VPS_HOST`.
- No token values, raw resource ids, internal URLs, cookies, credentials,
  database values, raw deployment objects, or log bodies were printed or stored.

Authenticated Coolify `GET` projection:

| Check | Result | Timing |
| --- | --- | ---: |
| `GET /api/v1/version` | pass | 404 ms |
| `GET /api/v1/teams/current` | pass | 135 ms |
| `GET /api/v1/projects/{configured-project-id}` | pass | 59 ms |
| `GET /api/v1/projects/{configured-project-id}/environments` | pass | 51 ms |
| `GET /api/v1/projects/{configured-project-id}/{configured-production-environment}` | pass | 258 ms |
| `GET /api/v1/resources` | pass | 276 ms |
| `GET /api/v1/deployments` | pass | 57 ms |

Production environment projection:

- applications: `6`
- application rows:
  `workers-backtest`, `soar-web`, `workers-market-stream`,
  `workers-execution`, `workers-market-data`, `soar-api`
- application statuses: `running:unknown` for all six rows
- application restart counts:
  `workers-backtest=0`, `soar-web=1`, `workers-market-stream=0`,
  `workers-execution=2`, `workers-market-data=0`, `soar-api=2`
- PostgreSQL resources: `1`, status `running:healthy`, restart count `52`
- Redis resources: `1`, status `running:healthy`, restart count `682`
- global visible resources: `17`
- visible deployments: `0`

The projection proves read-only Coolify reachability and no visible deployment
queue. Coolify application status fidelity remains limited because application
rows still report `running:unknown`; app smoke and protected workers readiness
remain the stronger runtime health evidence in this heartbeat.

## Validation

- `pnpm run -s ops:deploy:smoke -- --base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch`:
  first run public PASS, protected `/workers/ready` `401` through stale token
  path; rerun with token suppressed PASS including protected workers readiness.
- `node scripts/runProdUiModuleClickthroughAudit.mjs ...`: PASS.
- `pnpm run -s ops:coolify-stack:env-check:test`: PASS (`11/11`).
- `curl.exe` phase timing: API `/health` 20/20 `200`, API `/ready` 10/10
  `200`, Web `/` 10/10 `200`.
- Read-only Coolify `GET` projection: PASS.

## Disposition

[LUC-5387](/LUC/issues/LUC-5387) can close this routine heartbeat as a
read-only watch checkpoint:

- app reachable and authenticated clickthrough passed
- protected workers readiness passed via fresh login path
- Coolify/DB/Redis read-only health evidence is current
- no visible deployment queue exists
- residual API `/health` TLS/start-transfer tails remain a watch item, not a
  deploy/restart/Backend repair approval

