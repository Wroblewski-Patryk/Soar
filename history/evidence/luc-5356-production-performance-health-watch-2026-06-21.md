# LUC-5356 Production Performance And Server Health Watch

## Status

- Result: **INCIDENT_DELEGATED / PARTIALLY_VERIFIED / API_HEALTH_LATENCY_TAILS**
- Issue: [LUC-5356](/LUC/issues/LUC-5356)
- Follow-up: [LUC-5360](/LUC/issues/LUC-5360)
- Evidence window: 2026-06-20T22:28Z to 2026-06-20T22:30Z
- Generated by: 09 DRE (Deployment & Reliability Engineer)

## Scope

Read-only production performance and health checkpoint for Soar.

Included:

- public production smoke for API `/health`, API `/ready`, Web `/`, and Web
  `/api/build-info`
- five-sample public route timing for Web `/`, Web `/auth/login`,
  Web `/api/build-info`, API `/health`, and API `/ready`
- focused recheck for API `/health` and Web `/`
- production Web build-info readback
- read-only Coolify API projection for Soar production
- Coolify stack env-check contract tests and current-runner stack env preflight

Excluded:

- deploy, push, restart, rollback, environment edit, secret/account readback,
  database/Redis mutation, raw log capture, production account mutation,
  exchange/order/position/payment/subscription mutation, and live-trading action

## Public Smoke

Command:

```powershell
pnpm run -s ops:deploy:smoke -- --base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch --no-workers
```

Result:

- PASS API `/health` -> 200
- PASS API `/ready` -> 200
- PASS Web `/` -> 200
- PASS Web `/api/build-info` -> 200

## Build Info

Production Web `/api/build-info` returned:

- `gitSha`: `42177530f2a2ddc22832133b545bccab6ab404eb`
- `gitRef`: `main`
- `buildId`: `Urnq8xtZUh932c0e3vKGl`
- `metadataGeneratedAt`: `2026-06-15T21:00:54.489Z`
- `metadataSource`: `env-runtime`
- `checkedAt`: `2026-06-20T22:28:47.846Z`

`env-runtime` remains diagnostic-only provenance and is not treated as
release-grade build metadata.

## Route Timing

Five `curl.exe` samples per route, max time `25s`.

| Target | Statuses | Samples ms | Max ms | Avg ms | Result |
| --- | --- | ---: | ---: | ---: | --- |
| Web `/` | 200,200,200,200,200 | 601,482,589,3045,127 | 3045 | 968.8 | WARN |
| Web `/auth/login` | 200,200,200,200,200 | 243,126,770,439,1137 | 1137 | 543.0 | WARN |
| Web `/api/build-info` | 200,200,200,200,200 | 85,676,1523,559,873 | 1523 | 743.2 | WARN |
| API `/health` | 200,200,200,200,200 | 110,1507,1631,1675,7141 | 7141 | 2412.8 | WARN |
| API `/ready` | 200,200,200,200,200 | 83,84,575,456,1551 | 1551 | 549.8 | WARN |

Focused recheck:

| Target | Statuses | Samples ms | Max ms | Avg ms | >1000 ms | Result |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| API `/health` | 20/20 `200` | 72,100,78,186,105,125,228,164,141,147,148,666,87,1188,1307,111,2729,79,1968,99 | 2729 | 486.4 | 4 | WARN |
| Web `/` | 10/10 `200` | 122,119,213,132,180,189,164,183,162,184 | 213 | 164.8 | 0 | PASS |

Interpretation:

- Public app reachability is up.
- Web `/` normalized on focused recheck, so the one 3045 ms sample is watchful
  but not currently reproduced.
- API `/health` reproduced low-second latency tails despite returning `200`.
  Since `/health` is dependency-light, the current signal needs DRE/Ops
  edge/proxy/network/container correlation before treating it as backend
  readiness dependency behavior.

## Coolify / VPS Read-Only Projection

Secret handling:

- Required Coolify binding names are present by name only:
  `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_TOKEN`,
  `COOLIFY_SOAR_PROJECT_ID`, `COOLIFY_SOAR_PRODUCTION_ENVIRONMENT`,
  `COOLIFY_SOAR_TEAM_ID`, `COOLIFY_TEAM_ID`,
  `COOLIFY_SOAR_API_APP_ID`, `COOLIFY_SOAR_WEB_APP_ID`,
  `COOLIFY_SOAR_APP_ID`, `COOLIFY_SOAR_POSTGRES_RESOURCE_ID`, and
  `COOLIFY_SOAR_REDIS_RESOURCE_ID`.
- No token values, raw resource ids, internal URLs, cookies, credentials,
  database values, raw deployment objects, or log bodies were printed or stored.

Authenticated Coolify `GET` projection at `2026-06-20T22:29:39.121Z`:

| Check | Result |
| --- | --- |
| `GET /api/v1/version` | pass |
| `GET /api/v1/teams/current` | pass; selector `LuckySparrow` |
| `GET /api/v1/projects` | pass; `1` visible project row |
| `GET /api/v1/projects/{configured-project-id}` | pass; project `Soar` |
| `GET /api/v1/projects/{configured-project-id}/environments` | pass; `production` |
| `GET /api/v1/projects/{configured-project-id}/{configured-production-environment}` | pass |
| `GET /api/v1/resources` | pass; `1` visible global resource row |
| `GET /api/v1/deployments` | pass; `1` visible deployment row |

Production environment projection:

- applications: `6`
- application names:
  `workers-backtest`, `soar-web`, `workers-market-stream`,
  `workers-execution`, `workers-market-data`, `soar-api`
- application statuses: `running:unknown` for all six rows
- databases: `1`
- database status: `unknown`
- generic services: `0`

## Local Contract Checks

Command:

```powershell
pnpm run -s ops:coolify-stack:env-check:test
```

Result: PASS (`11/11`).

Current-runner stack env preflight:

```powershell
pnpm run -s ops:coolify-stack:env-check
```

Result: expected FAIL closed.

- required present: `0/16`
- secret handling: values redacted; only variable names reported

This is the Coolify service stack env family, not the Coolify API status
binding family used for the read-only status projection above.

## Disposition

[LUC-5356](/LUC/issues/LUC-5356) should close as delegated rather than
healthy. The watch produced useful read-only evidence, and the active
unresolved signal is narrow enough for one child incident:
[LUC-5360](/LUC/issues/LUC-5360) owns DRE/Ops correlation for recurring
production API `/health` low-second latency tails.

No duplicate broad server-health issue was created.

