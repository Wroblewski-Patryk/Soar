# LUC-5526 Production Performance And Server Health Watch

## Status

- Result: **DONE / PARTIALLY_VERIFIED / APP_HEALTHY / LATENCY_AND_TOOLING_WATCH**
- Issue: [LUC-5526](/LUC/issues/LUC-5526)
- Evidence window: 2026-06-27T15:00Z to 2026-06-27T15:05Z
- Generated by: 09 DRE (Deployment & Reliability Engineer)

## Scope

Read-only recurring production performance and server-health watch for Soar.

Included:

- public smoke for API `/health`, API `/ready`, Web `/`, and Web
  `/api/build-info`
- protected `/workers/ready` smoke through the pre-bound token path and the
  fresh login path
- protected rollback guard through fresh login path
- authenticated production UI route/module clickthrough
- authenticated dashboard/admin API timing for representative read-only
  endpoints
- read-only Coolify project/environment/resource/deployment projection
- package-manager fallback and process cleanup evidence

Excluded:

- deploy, push, restart, rollback execution, environment edit, secret/account
  readback, database/Redis mutation, raw log capture, screenshot capture,
  production account mutation, exchange action, order, position,
  payment/subscription mutation, or live-trading action

## Wake Context

The wake payload assigned [LUC-5526](/LUC/issues/LUC-5526) with no pending
comments and `fallbackFetchNeeded=false`. Checkout was already claimed by the
harness, so this heartbeat used the inline issue scope and did not call
checkout again.

## Smoke

Initial `pnpm run` attempts for deploy smoke and Coolify env-check tests timed
out after the package manager spawned non-interactive install/check processes.
The same scripts were then run directly with `node`.

Direct stale-token command:

```powershell
node scripts/deploySmokeCheck.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`
- FAIL API `/workers/ready` -> timeout after `8000 ms` on both attempts

Interpretation: the pre-bound `SMOKE_AUTH_TOKEN` path remains unreliable for
protected workers readiness in this runner.

Fresh-login command, with `SMOKE_AUTH_TOKEN` suppressed only for the child
process:

```powershell
node scripts/deploySmokeCheck.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- FAIL Web `/api/build-info` -> timeout after `8000 ms` on both attempts
- PASS API `/workers/ready` -> `200`

Interpretation: protected workers readiness passed through the fresh approved
login path. The single build-info timeout was investigated with follow-up
timing and did not reproduce as an outage.

## Build Info

Production Web `/api/build-info` returned:

- `gitSha`: `42177530f2a2ddc22832133b545bccab6ab404eb`
- `gitRef`: `main`
- `buildId`: `Urnq8xtZUh932c0e3vKGl`
- `metadataGeneratedAt`: `2026-06-15T21:00:54.489Z`
- `metadataSource`: `env-runtime`

`env-runtime` remains diagnostic-only provenance and is not release-grade build
metadata. Release provenance remains separate from this routine watch.

## Public Timing

PowerShell `Invoke-WebRequest` timing, ten samples each:

| Target | Statuses | Max ms | Avg ms | Samples ms |
| --- | --- | ---: | ---: | --- |
| Web `/api/build-info` | `200:10` | 2653.4 | 632.0 | 2653.4, 275.7, 384.3, 440.2, 520.5, 473.2, 382.4, 435.2, 364.6, 390.9 |
| API `/health` | `200:10` | 272.2 | 184.8 | 272.2, 178.2, 174.4, 156.4, 146.5, 150.4, 144.7, 246.2, 209.6, 169.8 |
| API `/ready` | `200:10` | 292.2 | 227.6 | 239.6, 141.8, 259.6, 244.3, 224.3, 211.6, 292.2, 233.7, 213.7, 215.6 |
| Web `/` | `200:10` | 1084.4 | 418.7 | 1013.8, 1084.4, 477.7, 339.5, 267.3, 360.0, 228.7, 130.8, 143.8, 141.4 |

Interpretation:

- No public HTTP failures were observed in the follow-up timing window.
- API health/readiness were stable and sub-300 ms.
- Web root and build-info showed low-second first-sample/cold outliers, then
  normalized.

## Authenticated Dashboard API Timing

Fresh login token source: `login`. Payloads were not stored. Three samples per
endpoint:

| Endpoint | Statuses | Max ms | Avg ms | Samples ms |
| --- | --- | ---: | ---: | --- |
| `/auth/me` | `200:3` | 343.8 | 325.2 | 301.3, 343.8, 330.4 |
| `/dashboard/bots` | `200:3` | 391.2 | 354.3 | 287.3, 391.2, 384.3 |
| `/dashboard/wallets` | `200:3` | 547.2 | 471.0 | 443.1, 547.2, 422.8 |
| `/dashboard/markets/universes` | `200:3` | 239.9 | 229.9 | 237.4, 212.3, 239.9 |
| `/dashboard/markets/catalog` | `200:3` | 1914.8 | 729.3 | 1914.8, 113.3, 159.8 |
| `/dashboard/strategies` | `200:3` | 202.7 | 169.8 | 157.1, 149.7, 202.7 |
| `/dashboard/strategies/indicators` | `200:3` | 304.2 | 223.9 | 304.2, 213.5, 154.1 |
| `/dashboard/backtests/runs` | `200:3` | 311.4 | 225.5 | 311.4, 171.1, 194.0 |
| `/dashboard/reports/cross-mode-performance` | `200:3` | 224.6 | 213.4 | 221.6, 194.0, 224.6 |
| `/dashboard/logs` | `200:3` | 243.8 | 226.2 | 243.8, 197.5, 237.2 |
| `/dashboard/profile/subscription` | `200:3` | 309.8 | 221.8 | 309.8, 156.9, 198.8 |
| `/admin/users` | `200:3` | 285.5 | 221.9 | 151.6, 285.5, 228.7 |
| `/admin/subscriptions/plans` | `200:3` | 337.9 | 312.1 | 299.6, 298.9, 337.9 |

Interpretation:

- Representative authenticated dashboard and admin API reads passed.
- One cold `/dashboard/markets/catalog` sample reached `1914.8 ms`, then
  normalized under `160 ms`.
- No dashboard 60-second-class stall or HTTP failure was reproduced.

## Authenticated UI Clickthrough

Command:

```powershell
node scripts/runProdUiModuleClickthroughAudit.mjs --web-base-url https://soar.luckysparrow.ch --api-base-url https://api.soar.luckysparrow.ch --output-json history/artifacts/luc-5526-prod-ui-module-clickthrough-2026-06-27.json --output-md history/evidence/luc-5526-prod-ui-module-clickthrough-2026-06-27.md --today 2026-06-27
```

Result: PASS.

- public routes: PASS `4/4`
- dashboard routes: PASS `18/18`
- admin routes: PASS `3/3`
- legacy redirects: PASS `3/3`
- dashboard auth: login present
- admin auth: provided present

Evidence:

- `history/evidence/luc-5526-prod-ui-module-clickthrough-2026-06-27.md`
- `history/artifacts/luc-5526-prod-ui-module-clickthrough-2026-06-27.json`

## Rollback Guard

Command:

```powershell
node scripts/evaluateRollbackGuard.mjs --base-url https://api.soar.luckysparrow.ch
```

Run with `ROLLBACK_GUARD_AUTH_EMAIL` / `ROLLBACK_GUARD_AUTH_PASSWORD` mapped
from the approved smoke credential family only in the child process.

Result:

- `shouldRollback`: `false`
- reasons: none
- workers ready: `ready`, topology `healthy`
- required worker families: `backtest`, `execution`, `market-data`,
  `market-stream`
- runtime freshness: `PASS`
- running runtime sessions: `5`
- stale session ids: none
- alerts: none

## Coolify / VPS Read-Only Projection

Secret handling:

- Present binding names were checked without values:
  `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_TOKEN`,
  `COOLIFY_SOAR_PROJECT_ID`, `COOLIFY_SOAR_PRODUCTION_ENVIRONMENT`,
  `COOLIFY_SOAR_API_APP_ID`, `COOLIFY_SOAR_WEB_APP_ID`,
  `COOLIFY_SOAR_POSTGRES_RESOURCE_ID`, `COOLIFY_SOAR_REDIS_RESOURCE_ID`, and
  `VPS_HOST`.
- No token values, raw resource ids, internal URLs, cookies, credentials,
  database values, raw deployment objects, or log bodies were printed or
  stored.

Authenticated Coolify `GET` projection:

| Check | Result | Timing |
| --- | --- | ---: |
| `GET /api/v1/version` | pass | 1245 ms |
| `GET /api/v1/teams/current` | pass | 178 ms |
| `GET /api/v1/projects/{configured-project-id}` | pass | 53 ms |
| `GET /api/v1/projects/{configured-project-id}/environments` | pass | 149 ms |
| `GET /api/v1/projects/{configured-project-id}/{configured-production-environment}` | pass | 158 ms |
| `GET /api/v1/resources` | pass | 343 ms |
| `GET /api/v1/deployments` | pass | 50 ms |

Production environment projection:

- applications: `6`
- application rows:
  `workers-backtest`, `soar-web`, `workers-market-stream`,
  `workers-execution`, `soar-api`, `workers-market-data`
- application statuses: `running:unknown` for all six rows
- application restart counts:
  `workers-backtest=0`, `soar-web=1`, `workers-market-stream=0`,
  `workers-execution=2`, `soar-api=2`, `workers-market-data=0`
- global resources: `17`
- visible deployments: `0`
- global PostgreSQL/Redis rows were visible as healthy in the allowlisted
  resource summary; exact per-row environment naming was limited by the
  PowerShell object shape.

Host-level VPS pressure, proxy/container-engine pressure, and sanitized
log-window capture were not attempted because this runner exposes `VPS_HOST`
but no approved `SSH*` or dedicated read-only `VPS_*` status credential family.

## Validation

- `node scripts/deploySmokeCheck.mjs --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch`:
  public PASS, protected `/workers/ready` timed out through pre-bound token.
- Same deploy smoke with token suppressed for fresh login:
  protected `/workers/ready` PASS; Web `/api/build-info` timed out during this
  run.
- Follow-up public timing:
  Web `/api/build-info`, API `/health`, API `/ready`, and Web `/` all returned
  `200:10`.
- `node scripts/evaluateRollbackGuard.mjs --base-url https://api.soar.luckysparrow.ch`:
  PASS, `shouldRollback=false`.
- `node scripts/runProdUiModuleClickthroughAudit.mjs ...`: PASS.
- Authenticated dashboard/admin API timing: PASS for all representative
  endpoints.
- `node --test scripts/checkCoolifyStackEnv.test.mjs`: PASS (`11/11`).
- Read-only Coolify `GET` projection: PASS.
- Package-manager path:
  initial `pnpm run` validation timed out after spawning install/check
  processes; direct `node` commands supplied the evidence for this heartbeat.
- Cleanup:
  task-owned stale `pnpm`/install processes were identified by command line and
  stopped narrowly; no `chrome-headless-shell`, Chromium, or Chrome validation
  processes remained.

## Disposition

[LUC-5526](/LUC/issues/LUC-5526) can close this routine heartbeat as a
read-only watch checkpoint:

- app reachable and authenticated clickthrough passed
- representative authenticated dashboard/admin API reads passed
- protected workers readiness passed via fresh login path
- rollback guard passed with `shouldRollback=false`
- Coolify read-only projection is current, and no visible deployment queue
  exists
- residual stale-token timeout and low-second Web/build-info/dashboard cold
  samples remain watch items
- no duplicate incident was created because there is no current outage,
  rollback trigger, persistent protected diagnostic failure through fresh
  login, or reproduced 60-second dashboard stall
- no deploy, push, restart, rollback, env edit, DB/Redis mutation, raw log
  capture, secret/account readback, production account mutation, exchange
  action, order, position, payment/subscription mutation, or live-trading
  action occurred
