# LUC-5429 Production Performance And Server Health Watch

## Status

- Result: **DONE / VERIFIED_READ_ONLY / APP_HEALTHY / TOKEN_STALE_RESIDUAL**
- Issue: [LUC-5429](/LUC/issues/LUC-5429)
- Evidence window: 2026-06-21T02:27Z to 2026-06-21T02:29Z
- Generated by: 09 DRE (Deployment & Reliability Engineer)

## Scope

Read-only recurring production performance and server-health watch for Soar.

Included:

- public smoke for API `/health`, API `/ready`, Web `/`, Web `/auth/login`,
  and Web `/api/build-info`
- protected `/workers/ready` smoke through stale token path and fresh login path
- protected rollback guard through fresh login path
- authenticated production UI route/module clickthrough
- authenticated dashboard/admin API timing for representative read-only
  endpoints
- read-only Coolify project/environment/resource/deployment projection
- Coolify stack env checker unit proof

Excluded:

- deploy, push, restart, rollback, environment edit, secret/account readback,
  database/Redis mutation, raw log capture, screenshot capture, production
  account mutation, exchange action, order, position, payment/subscription
  mutation, or live-trading action

## Wake Context

The wake payload assigned [LUC-5429](/LUC/issues/LUC-5429) with no pending
comments and `fallbackFetchNeeded=false`. Checkout was already claimed by the
harness, so this heartbeat used the inline issue scope and did not call
checkout again.

## Smoke

First command:

```powershell
pnpm run -s ops:deploy:smoke -- --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`
- FAIL API `/workers/ready` -> `401`

Interpretation: the pre-bound `SMOKE_AUTH_TOKEN` path is stale or not accepted
for protected workers readiness in this runner.

Second command with `SMOKE_AUTH_TOKEN` suppressed only in the child process so
the script uses env-bound email/password login:

```powershell
pnpm run -s ops:deploy:smoke -- --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch
```

Result:

- PASS API `/health` -> `200`
- PASS API `/ready` -> `200`
- PASS Web `/` -> `200`
- PASS Web `/api/build-info` -> `200`
- PASS API `/workers/ready` -> `200`

## Build Info

Production Web `/api/build-info` returned:

- `gitSha`: `42177530f2a2ddc22832133b545bccab6ab404eb`
- `gitRef`: `main`
- `buildId`: `Urnq8xtZUh932c0e3vKGl`
- `metadataGeneratedAt`: `2026-06-15T21:00:54.489Z`
- `metadataSource`: `env-runtime`
- `checkedAt`: `2026-06-21T02:28:51.012Z`

`env-runtime` remains diagnostic-only provenance and is not release-grade build
metadata. Release provenance remains routed through [LUC-4912](/LUC/issues/LUC-4912).

## Public Timing

PowerShell `Invoke-WebRequest` timing, ten samples each:

| Target | Statuses | Max ms | Avg ms | Samples ms |
| --- | --- | ---: | ---: | --- |
| Web `/` | `200:10` | 3503.9 | 1010.1 | 710.4, 1229.1, 2033.4, 3503.9, 139.3, 183.4, 1103.9, 532.6, 509.1, 156.3 |
| Web `/auth/login` | `200:10` | 1134.6 | 324.4 | 137.8, 639.8, 88.8, 109.7, 123.4, 176.5, 120.5, 248.8, 1134.6, 464.5 |
| Web `/api/build-info` | `200:10` | 2426.2 | 1001.5 | 109.0, 118.7, 85.3, 1852.8, 2426.2, 969.8, 1139.3, 2012.2, 1152.6, 148.8 |
| API `/health` | `200:10` | 1137.8 | 306.9 | 376.9, 1137.8, 870.3, 105.0, 72.4, 258.5, 62.6, 56.6, 75.8, 53.0 |
| API `/ready` | `200:10` | 143.9 | 60.9 | 86.2, 48.7, 40.9, 41.1, 33.0, 41.0, 143.9, 53.4, 69.3, 51.1 |

Interpretation:

- No public HTTP failures were observed.
- API readiness is fast and stable in this window.
- Web `/` and Web `/api/build-info` had low-second outliers but no
  operator-reported 60-second class stall.

## Authenticated Dashboard API Timing

Fresh login token source: `login`. Payloads were not stored. Three samples per
endpoint:

| Endpoint | OK | Statuses | Max ms | Avg ms | Samples ms |
| --- | --- | --- | ---: | ---: | --- |
| `/auth/me` | 3/3 | `200` | 82.9 | 44.3 | 82.9, 25.1, 25.0 |
| `/dashboard/bots` | 3/3 | `200` | 50.8 | 37.8 | 50.8, 28.7, 33.9 |
| `/dashboard/wallets` | 3/3 | `200` | 27.3 | 26.3 | 27.3, 25.4, 26.1 |
| `/dashboard/markets/universes` | 3/3 | `200` | 25.7 | 24.1 | 25.7, 24.6, 22.1 |
| `/dashboard/markets/catalog` | 3/3 | `200` | 1233.4 | 429.4 | 1233.4, 27.9, 26.8 |
| `/dashboard/strategies` | 3/3 | `200` | 26.5 | 25.7 | 25.7, 24.9, 26.5 |
| `/dashboard/strategies/indicators` | 3/3 | `200` | 27.3 | 25.3 | 23.4, 27.3, 25.1 |
| `/dashboard/backtests/runs` | 3/3 | `200` | 33.0 | 29.7 | 33.0, 27.2, 28.8 |
| `/dashboard/reports/cross-mode-performance` | 3/3 | `200` | 67.4 | 50.4 | 67.4, 43.4, 40.3 |
| `/dashboard/logs` | 3/3 | `200` | 39.7 | 38.5 | 39.7, 38.6, 37.3 |
| `/dashboard/profile/subscription` | 3/3 | `200` | 50.0 | 37.0 | 50.0, 32.8, 28.1 |
| `/admin/users` | 3/3 | `200` | 30.7 | 28.3 | 29.0, 30.7, 25.3 |
| `/admin/subscriptions/plans` | 3/3 | `200` | 30.6 | 27.7 | 30.6, 25.7, 26.7 |

Interpretation:

- Representative authenticated dashboard and admin API reads passed.
- One cold `/dashboard/markets/catalog` sample reached `1233.4 ms`, then
  normalized under `30 ms`.
- The operator-reported approximately 60-second dashboard load was not
  reproduced by route clickthrough or these API timing checks.

## Authenticated UI Clickthrough

Command:

```powershell
node scripts/runProdUiModuleClickthroughAudit.mjs --web-base-url https://soar.luckysparrow.ch --api-base-url https://api.soar.luckysparrow.ch --output-json history/artifacts/luc-5429-prod-ui-module-clickthrough-2026-06-21.json --output-md history/evidence/luc-5429-prod-ui-module-clickthrough-2026-06-21.md --today 2026-06-21
```

Result: PASS.

- public routes: PASS `4/4`
- dashboard routes: PASS `18/18`
- admin routes: PASS `3/3`
- legacy redirects: PASS `3/3`
- dashboard auth: login present
- admin auth: provided present

Evidence:

- `history/evidence/luc-5429-prod-ui-module-clickthrough-2026-06-21.md`
- `history/artifacts/luc-5429-prod-ui-module-clickthrough-2026-06-21.json`

## Rollback Guard

Command:

```powershell
pnpm run -s ops:deploy:rollback-guard -- --base-url https://api.soar.luckysparrow.ch
```

Run with stale token suppressed and `ROLLBACK_GUARD_AUTH_EMAIL` /
`ROLLBACK_GUARD_AUTH_PASSWORD` mapped from the approved smoke credential
family.

Result:

- `shouldRollback`: `false`
- reasons: none
- workers ready: `ready`, topology `healthy`
- required worker families: `backtest`, `execution`, `market-data`,
  `market-stream`
- runtime freshness: `PASS`
- running runtime sessions: `5`
- stale session ids: none
- alerts: none

## Coolify / VPS Read-Only Projection

Secret handling:

- Present binding names were checked without values:
  `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_TOKEN`,
  `COOLIFY_SOAR_PROJECT_ID`, `COOLIFY_SOAR_PRODUCTION_ENVIRONMENT`,
  `COOLIFY_SOAR_API_APP_ID`, `COOLIFY_SOAR_WEB_APP_ID`,
  `COOLIFY_SOAR_POSTGRES_RESOURCE_ID`, `COOLIFY_SOAR_REDIS_RESOURCE_ID`, and
  `VPS_HOST`.
- No token values, raw resource ids, internal URLs, cookies, credentials,
  database values, raw deployment objects, or log bodies were printed or
  stored.

Authenticated Coolify `GET` projection:

| Check | Result | Timing |
| --- | --- | ---: |
| `GET /api/v1/version` | pass | 226 ms |
| `GET /api/v1/teams/current` | pass | 76 ms |
| `GET /api/v1/projects/{configured-project-id}` | pass | 56 ms |
| `GET /api/v1/projects/{configured-project-id}/environments` | pass | 51 ms |
| `GET /api/v1/projects/{configured-project-id}/{configured-production-environment}` | pass | 151 ms |
| `GET /api/v1/resources` | pass | 281 ms |
| `GET /api/v1/deployments` | pass | 43 ms |

Production environment projection:

- applications: `6`
- application rows:
  `soar-web`, `workers-backtest`, `workers-market-stream`,
  `workers-execution`, `soar-api`, `workers-market-data`
- application statuses: `running:unknown` for all six rows
- application restart counts:
  `soar-web=1`, `workers-backtest=0`, `workers-market-stream=0`,
  `workers-execution=2`, `soar-api=2`, `workers-market-data=0`
- global resources: `17`
- visible deployments: `0`
- global PostgreSQL/Redis rows visible:
  `postgresql` `running:healthy` restart count `52`;
  `redis` `running:healthy` restart count `682`

The projection proves read-only Coolify reachability and no visible deployment
queue. Coolify application status fidelity remains limited because application
rows still report `running:unknown`; app smoke and protected workers readiness
remain the stronger runtime health evidence in this heartbeat.

Host-level VPS pressure, proxy/container-engine pressure, and sanitized
log-window capture were not attempted because this runner exposes `VPS_HOST`
but no approved `SSH*` or dedicated read-only `VPS_*` status credential family.

## Validation

- `pnpm run -s ops:deploy:smoke -- --api-base-url https://api.soar.luckysparrow.ch --web-base-url https://soar.luckysparrow.ch`:
  first run public PASS, protected `/workers/ready` `401` through stale token
  path; rerun with token suppressed PASS including protected workers readiness.
- `pnpm run -s ops:deploy:rollback-guard -- --base-url https://api.soar.luckysparrow.ch`:
  first run failed closed through stale token path; rerun with fresh login path
  PASS and `shouldRollback=false`.
- `node scripts/runProdUiModuleClickthroughAudit.mjs ...`: PASS.
- Authenticated dashboard/admin API timing: PASS for all representative
  endpoints.
- `pnpm run -s ops:coolify-stack:env-check:test`: PASS (`11/11`).
- Read-only Coolify `GET` projection: PASS.

## Disposition

[LUC-5429](/LUC/issues/LUC-5429) can close this routine heartbeat as a
read-only watch checkpoint:

- app reachable and authenticated clickthrough passed
- representative authenticated dashboard/admin API reads passed
- protected workers readiness passed via fresh login path
- rollback guard passed with `shouldRollback=false`
- Coolify read-only projection is current, and no visible deployment queue
  exists
- residual stale-token path should remain a watch item, but it is not an app
  outage because fresh approved login proof passes protected diagnostics
- no duplicate incident was created because there is no current outage,
  no 60-second stall reproduction, and no confirmed app-handler-only delay
