# Test System

Required test layers:

- `unit/` - isolated function and module behavior.
- `integration/` - module contracts, database, API, queue, and service interactions.
- `e2e/` - complete user or system workflows.
- `ai-behavior/` - agent behavior, prompt workflows, and conversation simulations.

Every changed behavior needs a relevant test or a backlog task explaining the gap.
