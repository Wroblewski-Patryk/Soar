# Obsidian Inbox

This folder contains unnamed Obsidian working files moved out of the docs root during cleanup.

Review these files before deleting them. Empty `.canvas` files and default `.base` files usually came from accidental Obsidian clicks.
