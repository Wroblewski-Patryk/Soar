# Deploy Lag - b414e523

Date: 2026-05-10

## Scope

Record production deploy freshness after the live order cancel boundary commit.

## Commit

- Full SHA: `b414e523`
- Commit: `feat(exchange): enable live order cancel boundary`

## Evidence

- BLOCKED: production Web build-info did not expose `b414e523` within the
  900-second wait window.
- Last observed production Web build-info:
  `bebe1906b764a74a641fe716e5d6fb3efe6d9f4d`.
- PASS: public deploy smoke after the timeout:
  - API `/health` -> HTTP 200
  - API `/ready` -> HTTP 200
  - Web `/` -> HTTP 200

## Meaning

The code for canonical exchange-side `LIVE_ORDER_CANCEL` is committed and
pushed to `main`, and local validation passed, but production is not yet proven
to contain that runtime behavior.

This is a deploy-freshness blocker, not a public service health failure.
