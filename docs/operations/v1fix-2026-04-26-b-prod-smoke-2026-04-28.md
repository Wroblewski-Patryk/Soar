# V1FIX-2026-04-26-B PROD Smoke

Date: 2026-04-28  
Environment: `prod`  
Candidate SHA: `25276b475937d9dcf4af6337abf10185ec7dcd0c`

## Scope

Post-deploy production smoke after pushing the latest repository state that
contained:
- scoped open-position uniqueness and wallet-first manual-order conflict scope
- orphan-repair and takeover hardening
- backtests closure pack already marked deploy-ready locally

This smoke focused on:
- public health/readiness baseline
- deployed SHA truth
- authenticated affected-account dashboard/runtime visibility on the live bot

## Public Baseline

- `GET https://api.soar.luckysparrow.ch/health` -> `200 OK`
- `GET https://api.soar.luckysparrow.ch/ready` -> `200 OK`
- `GET https://soar.luckysparrow.ch/` -> `200 OK`
- `GET https://soar.luckysparrow.ch/auth/login` -> `200 OK`
- unauthenticated `GET https://soar.luckysparrow.ch/dashboard` -> `307 /auth/login`
- unauthenticated `GET https://api.soar.luckysparrow.ch/metrics` -> `401 Unauthorized`

## Deployed SHA Proof

- `GET https://soar.luckysparrow.ch/api/build-info` returned:
  - `gitSha=25276b475937d9dcf4af6337abf10185ec7dcd0c`
  - `gitRef=main`
  - `metadataSource=env-runtime`

This matches the pushed release commit exactly.

## Authenticated Account Smoke

Login was verified successfully against production API for:
- account: `wroblewskipatryk@gmail.com`
- role: `ADMIN`

After establishing the authenticated browser context, `GET /dashboard` loaded
successfully on the affected account and exposed live runtime truth for the
selected live bot:

- selected bot showed `live (LIVE · Binance Futures Live)`
- runtime status showed `RUNNING`
- mode showed `LIVE`
- wallet showed `Binance Futures Live`
- capital source showed `Authenticated exchange balance`
- account balance rendered as `1.22 USDT`
- open positions table rendered one managed live position:
  - symbol: `DOGEUSDT`
  - side: `SHORT`
  - status badge: `Managed`
- market/runtime cards rendered symbol snapshots for:
  - `DOGEUSDT`
  - `BNBUSDT`
  - `XRPUSDT`
  - `ETHUSDT`

## Result

Status: `PASS`

The production environment is serving the expected SHA, public health gates are
green, protected routes remain protected, and the affected authenticated live
account now shows the live bot runtime with the `DOGEUSDT` managed position
visible on the dashboard.

## Notes

- This smoke intentionally did not place any manual orders or execute any live
  trading action.
- The verified result is limited to read-path/runtime visibility and production
  deploy truth after the release push.
