name: Promote PROD

on:
  workflow_dispatch:

concurrency:
  group: promote-prod
  cancel-in-progress: false

jobs:
  promote-prod:
    name: Promote verified SHA to production
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Trigger Coolify PROD deploy webhook
        env:
          PROD_DEPLOY_WEBHOOK_URL: ${{ secrets.COOLIFY_PROD_DEPLOY_HOOK_URL }}
          GIT_SHA: ${{ github.sha }}
          GIT_REF: ${{ github.ref }}
          REPOSITORY: ${{ github.repository }}
          ACTOR: ${{ github.actor }}
        run: |
          if [ -z "$PROD_DEPLOY_WEBHOOK_URL" ]; then
            echo "::error::Missing required secret COOLIFY_PROD_DEPLOY_HOOK_URL"
            exit 1
          fi

          payload=$(cat <<EOF
          {"sha":"$GIT_SHA","ref":"$GIT_REF","repository":"$REPOSITORY","actor":"$ACTOR","environment":"production","source":"manual-production-only"}
          EOF
          )

          http_code=$(curl -sS -o response.txt -w "%{http_code}" \
            -X POST "$PROD_DEPLOY_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            --data "$payload")

          echo "Coolify response code: $http_code"
          cat response.txt || true

          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::error::Production deploy webhook failed with HTTP $http_code"
            exit 1
          fi

      - name: Wait for web build-info to expose promoted SHA
        env:
          PROD_WEB_BASE_URL: ${{ secrets.PROD_WEB_BASE_URL }}
          GIT_SHA: ${{ github.sha }}
        run: |
          WEB_BASE_URL="${PROD_WEB_BASE_URL:-https://soar.luckysparrow.ch}"

          pnpm run ops:deploy:wait-web-build-info -- \
            --web-base-url "$WEB_BASE_URL" \
            --expected-sha "$GIT_SHA" \
            --timeout-seconds 900 \
            --interval-seconds 15

      - name: Mandatory post-deploy runtime freshness gate
        env:
          PROD_API_BASE_URL: ${{ secrets.PROD_API_BASE_URL }}
          PROD_RUNTIME_FRESHNESS_TOKEN: ${{ secrets.PROD_RUNTIME_FRESHNESS_TOKEN }}
        run: |
          if [ -z "$PROD_API_BASE_URL" ]; then
            echo "::error::Missing required secret PROD_API_BASE_URL"
            exit 1
          fi
          if [ -z "$PROD_RUNTIME_FRESHNESS_TOKEN" ]; then
            echo "::error::Missing required secret PROD_RUNTIME_FRESHNESS_TOKEN"
            exit 1
          fi

          max_attempts=20
          delay_seconds=30
          attempt=1

          while [ "$attempt" -le "$max_attempts" ]; do
            echo "Runtime freshness check attempt $attempt/$max_attempts"
            if pnpm run ops:deploy:runtime-freshness -- --base-url "$PROD_API_BASE_URL" --auth-token "$PROD_RUNTIME_FRESHNESS_TOKEN"; then
              echo "Runtime freshness gate PASS"
              exit 0
            fi
            if [ "$attempt" -eq "$max_attempts" ]; then
              echo "::error::Runtime freshness gate failed after $max_attempts attempts"
              exit 1
            fi
            sleep "$delay_seconds"
            attempt=$((attempt + 1))
          done

      - name: Evaluate rollback guard trigger conditions
        env:
          PROD_API_BASE_URL: ${{ secrets.PROD_API_BASE_URL }}
          PROD_RUNTIME_FRESHNESS_TOKEN: ${{ secrets.PROD_RUNTIME_FRESHNESS_TOKEN }}
        run: |
          pnpm run ops:deploy:rollback-guard -- --base-url "$PROD_API_BASE_URL" --auth-token "$PROD_RUNTIME_FRESHNESS_TOKEN"

      - name: Trigger automatic rollback on runtime-critical gate failure
        if: failure()
        env:
          PROD_ROLLBACK_WEBHOOK_URL: ${{ secrets.COOLIFY_PROD_ROLLBACK_HOOK_URL }}
          FAILED_SHA: ${{ github.sha }}
          FAILED_WORKFLOW: promote-prod
          FAILED_RUN_ID: ${{ github.run_id }}
          REPOSITORY: ${{ github.repository }}
          ACTOR: ${{ github.actor }}
        run: |
          if [ -z "$PROD_ROLLBACK_WEBHOOK_URL" ]; then
            echo "::error::Missing required secret COOLIFY_PROD_ROLLBACK_HOOK_URL"
            exit 1
          fi

          payload=$(cat <<EOF
          {"failedSha":"$FAILED_SHA","failedWorkflow":"$FAILED_WORKFLOW","failedRunId":"$FAILED_RUN_ID","repository":"$REPOSITORY","actor":"$ACTOR","environment":"production","action":"rollback_to_previous_stable"}
          EOF
          )

          http_code=$(curl -sS -o response.txt -w "%{http_code}" \
            -X POST "$PROD_ROLLBACK_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            --data "$payload")

          echo "Coolify rollback response code: $http_code"
          cat response.txt || true

          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::error::Automatic rollback webhook failed with HTTP $http_code"
            exit 1
          fi
